(* Auto-generated by tools/generate_modula2_bindings.py — Situation 2.4.357 (Consolidate color-space math into situation_impl_color.h; trace table regenerated.) *)
(* Do not edit by hand. *)

DEFINITION MODULE SituationCallbacks;

FROM SYSTEM IMPORT ADDRESS;
FROM SituationTypes IMPORT REALPtr, SituationModifiers, SituationSeekOrigin, SituationStreamResult, SituationStreamSize, Vector2;

(* Callback typedefs from sit/situation_api.h and sit/situation_base_callbacks.h *)

TYPE
  SituationFileDropCallback = PROCEDURE (INTEGER, ADDRESS, ADDRESS);
  SituationFileLoadCallback = PROCEDURE (ADDRESS, ADDRESS, ADDRESS);
  SituationFileSaveCallback = PROCEDURE (BOOLEAN, ADDRESS);
  SituationFileTextLoadCallback = PROCEDURE (ADDRESS, ADDRESS);
  SituationFocusCallback = PROCEDURE (BOOLEAN, ADDRESS);
  SituationMaximizeCallback = PROCEDURE (BOOLEAN, ADDRESS);
  SituationWindowCloseCallback = PROCEDURE (ADDRESS);
  SituationKeyCallback = PROCEDURE (INTEGER, INTEGER, INTEGER, SituationModifiers, ADDRESS);
  SituationCharCallback = PROCEDURE (CARDINAL, ADDRESS);
  SituationMouseButtonCallback = PROCEDURE (INTEGER, INTEGER, SituationModifiers, ADDRESS);
  SituationCursorPosCallback = PROCEDURE (Vector2, ADDRESS);
  SituationScrollCallback = PROCEDURE (Vector2, ADDRESS);
  SituationJoystickCallback = PROCEDURE (INTEGER, INTEGER, ADDRESS);
  SituationStreamReadCallback = PROCEDURE (ADDRESS, ADDRESS, SituationStreamSize) : SituationStreamSize;
  SituationStreamSeekCallback = PROCEDURE (ADDRESS, LONGINT, SituationSeekOrigin) : SituationStreamResult;
  SituationAudioCaptureCallback = PROCEDURE (REALPtr, CARDINAL, ADDRESS);
  SituationAudioProcessorCallback = PROCEDURE (REALPtr, CARDINAL, CARDINAL, CARDINAL, ADDRESS);
  GLFWerrorfunCallback = PROCEDURE (INTEGER, ADDRESS);

END SituationCallbacks.
