// Auto-generated by tools/generate_rust_bindings.py — Situation 2.4.357 (Consolidate color-space math into situation_impl_color.h; trace table regenerated.)
// Do not edit by hand.

use std::os::raw::{c_char, c_int, c_uint, c_void, c_long};

use crate::situation_ffi::*;
use crate::situation_types::*;
use crate::situation_callbacks::*;
use std::ffi::CStr;

// Helpers replacing C preprocessor macros & providing safe idiomatic wrappers

/// [GL+VK] Prepare the backend for a new frame of rendering commands.
pub fn situation_acquire_frame_command_buffer() -> Result<(), SituationError> {
    let err = unsafe { SituationAcquireFrameCommandBuffer() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Adds multiple dependencies for a single dependent job.
pub fn situation_add_job_dependencies(pool: *mut SituationThreadPool, prerequisites: &mut SituationJobId, count: c_int, dependent_job: SituationJobId) -> Result<(), SituationError> {
    let err = unsafe { SituationAddJobDependencies(pool, prerequisites, count, dependent_job) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Adds a dependency between two jobs (prereq -> dependent).
pub fn situation_add_job_dependency(pool: *mut SituationThreadPool, prerequisite_job: SituationJobId, dependent_job: SituationJobId) -> Result<(), SituationError> {
    let err = unsafe { SituationAddJobDependency(pool, prerequisite_job, dependent_job) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Manually apply the appropriate window state profile based on current focus.
pub fn situation_apply_current_profile_window_state() -> Result<(), SituationError> {
    let err = unsafe { SituationApplyCurrentProfileWindowState() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Attach a custom DSP processor to a sound's effect chain.
pub fn situation_attach_audio_processor(sound: &mut SituationSound, processor: SituationAudioProcessorCallback, user_data: *mut c_void) -> Result<(), SituationError> {
    let err = unsafe { SituationAttachAudioProcessor(sound, processor, user_data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Convenience: auto-select first available MIDI input. Equivalent to EnableMidiControl(..., -1).
pub fn situation_auto_connect_midi(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationAutoConnectMidi(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Upload bitmap_data as a NEAREST-filtered grid atlas for GPU text.
pub fn situation_bake_bitmap_font_atlas(font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationBakeBitmapFontAtlas(font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Rasterize a font into a GPU-ready atlas at the given size.
pub fn situation_bake_font_atlas(font: &mut SituationFont, fontSizePixels: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationBakeFontAtlas(font, fontSizePixels) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Start non-blocking GLSL load: [OpenGL] async compile/link; [Vulkan] shaderc on worker thread, pipelines on next frames. Poll with SituationPollShaderLoad.
pub fn situation_begin_load_shader_from_memory(vs_code: &CStr, fs_code: &CStr, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationBeginLoadShaderFromMemory(vs_code.as_ptr(), fs_code.as_ptr(), out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Vulkan] Non-blocking pipeline build from in-memory SPIR-V (bytecode copied). [OpenGL] blocking SPIR-V load (unchanged).
pub fn situation_begin_load_shader_from_spirv_memory(vs_spirv: *mut c_void, vs_len: usize, fs_spirv: *mut c_void, fs_len: usize, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationBeginLoadShaderFromSpirvMemory(vs_spirv, vs_len, fs_spirv, fs_len, out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Vulkan] Async SPIR-V with layout profile (e.g. UBO_SSBO for Demon Hunt). [OpenGL] profile ignored; same as SituationBeginLoadShaderFromSpirvMemory.
pub fn situation_begin_load_shader_from_spirv_memory_ex(vs_spirv: *mut c_void, vs_len: usize, fs_spirv: *mut c_void, fs_len: usize, layout_profile: SituationSpirvLayoutProfile, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationBeginLoadShaderFromSpirvMemoryEx(vs_spirv, vs_len, fs_spirv, fs_len, layout_profile, out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] glShaderStorageBlockBinding for SPIR-V when reflection reports binding 0 for layout(binding=N).
pub fn situation_bind_shader_storage_block(shader: SituationShader, block_name: &CStr, binding_point: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationBindShaderStorageBlock(shader, block_name.as_ptr(), binding_point) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] glUniformBlockBinding for std140 UBO blocks (layout(binding=N)).
pub fn situation_bind_uniform_block(shader: SituationShader, block_name: &CStr, binding_point: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationBindUniformBlock(shader, block_name.as_ptr(), binding_point) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Copies raw byte data into a specific region of an image.
pub fn situation_blit_raw_data_to_image(data: *mut c_void, x: c_int, y: c_int, width: c_int, height: c_int, src_channels: c_int) {
    unsafe { SituationBlitRawDataToImage(data, x, y, width, height, src_channels) };
}

/// All logical CPUs on a NUMA node
pub fn situation_build_numa_node_mask(numa_node_index: c_int) -> u64 {
    unsafe { SituationBuildNumaNodeMask(numa_node_index) }
}

/// All logical CPUs on one physical core
pub fn situation_build_physical_core_mask(physical_core_index: c_int) -> u64 {
    unsafe { SituationBuildPhysicalCoreMask(physical_core_index) }
}

/// One LP per core
pub fn situation_build_unique_core_mask(start_physical_core: c_int, count: c_int, avoid_siblings: bool) -> u64 {
    unsafe { SituationBuildUniqueCoreMask(start_physical_core, count, avoid_siblings) }
}

pub fn situation_camera_build_inv_view_proj(desc: *mut SituationCameraDesc, out_inv_vp: Mat4) {
    unsafe { SituationCameraBuildInvViewProj(desc, out_inv_vp) };
}

pub fn situation_camera_build_proj(desc: *mut SituationCameraDesc, out_proj: Mat4) {
    unsafe { SituationCameraBuildProj(desc, out_proj) };
}

pub fn situation_camera_build_view(desc: *mut SituationCameraDesc, out_view: Mat4) {
    unsafe { SituationCameraBuildView(desc, out_view) };
}

pub fn situation_camera_build_view_proj(desc: *mut SituationCameraDesc, out_vp: Mat4) {
    unsafe { SituationCameraBuildViewProj(desc, out_vp) };
}

pub fn situation_camera_unproject_pixel(desc: *mut SituationCameraDesc, inv_vp: Mat4, pixel: Vector2, framebuffer_px: Vector2, out_ray_origin: &mut Vector3, out_ray_dir: &mut Vector3) {
    unsafe { SituationCameraUnprojectPixel(desc, inv_vp, pixel, framebuffer_px, out_ray_origin, out_ray_dir) };
}

/// Cancel an active learn operation.
pub fn situation_cancel_midi_learn(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationCancelMidiLearn(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Checks all tracked resources for file changes and reloads them if necessary.
pub fn situation_check_hot_reloads() -> Result<(), SituationError> {
    let err = unsafe { SituationCheckHotReloads() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Clear all learned mappings for a node.
pub fn situation_clear_all_midi_mappings(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationClearAllMidiMappings(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Clear a specific learned CC mapping.
pub fn situation_clear_midi_mapping(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, control_index: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationClearMidiMapping(graph, handle, control_index) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Clear window configuration state flags.
pub fn situation_clear_window_state(flags: u32) {
    unsafe { SituationClearWindowState(flags) };
}

pub fn situation_cmd_begin_debug_group(cmd: SituationCommandBuffer, name: &CStr, color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBeginDebugGroup(cmd, name.as_ptr(), color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] Begins a render pass with detailed configuration.
pub fn situation_cmd_begin_render_pass(cmd: SituationCommandBuffer, info: *mut SituationRenderPassInfo) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBeginRenderPass(cmd, info) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_begin_render_to_display(cmd: SituationCommandBuffer, display_id: c_int, clear_color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBeginRenderToDisplay(cmd, display_id, clear_color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_bind_compute_buffer(cmd: SituationCommandBuffer, binding: u32, buffer: SituationBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindComputeBuffer(cmd, binding, buffer) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Bind a compute pipeline for a subsequent dispatch.
pub fn situation_cmd_bind_compute_pipeline(cmd: SituationCommandBuffer, pipeline: SituationComputePipeline) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindComputePipeline(cmd, pipeline) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Binds a texture as a storage image for compute shaders.
pub fn situation_cmd_bind_compute_texture(cmd: SituationCommandBuffer, binding: u32, texture: SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindComputeTexture(cmd, binding, texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] [Core] Binds a buffer's descriptor set (UBO/SSBO) to a set index.
pub fn situation_cmd_bind_descriptor_set(cmd: SituationCommandBuffer, set_index: u32, buffer: SituationBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindDescriptorSet(cmd, set_index, buffer) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] [Core] Binds a dynamic buffer descriptor set with an offset.
pub fn situation_cmd_bind_descriptor_set_dynamic(cmd: SituationCommandBuffer, set_index: u32, buffer: SituationBuffer, dynamic_offset: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindDescriptorSetDynamic(cmd, set_index, buffer, dynamic_offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Bind a 32-bit index buffer (SIT_INDEX_UINT32). Pass offset 0 when indices start at the beginning of the buffer.
pub fn situation_cmd_bind_index_buffer(cmd: SituationCommandBuffer, buffer: SituationBuffer, offset: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindIndexBuffer(cmd, buffer, offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Bind index buffer with 16- or 32-bit element type for subsequent SituationCmdDrawIndexed.
pub fn situation_cmd_bind_index_buffer_ex(cmd: SituationCommandBuffer, buffer: SituationBuffer, offset: usize, index_type: SituationIndexType) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindIndexBufferEx(cmd, buffer, offset, index_type) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [VK+GL] Push mesh vertex/index BDA block for pull shaders (SituationMeshPullPushConstants @ offset 0). Requires SIT_FEATURE_BINDLESS_BUFFERS.
pub fn situation_cmd_bind_mesh_pull_buffers(cmd: SituationCommandBuffer, mesh: SituationMesh) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindMeshPullBuffers(cmd, mesh) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] Binds a graphics pipeline (shader program) for subsequent draws.
pub fn situation_cmd_bind_pipeline(cmd: SituationCommandBuffer, shader: SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindPipeline(cmd, shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Binds a texture as a sampled image (sampler2D) to a binding point.
pub fn situation_cmd_bind_sampled_texture(cmd: SituationCommandBuffer, binding: c_int, texture: SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindSampledTexture(cmd, binding, texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_bind_texture(cmd: SituationCommandBuffer, set_index: u32, texture: SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindTexture(cmd, set_index, texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] [Core] Binds a texture's descriptor set (sampler/storage) to a set index.
pub fn situation_cmd_bind_texture_set(cmd: SituationCommandBuffer, set_index: u32, texture: SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindTextureSet(cmd, set_index, texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_bind_uniform_buffer(cmd: SituationCommandBuffer, contract_id: u32, buffer: SituationBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindUniformBuffer(cmd, contract_id, buffer) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Bind a vertex buffer for subsequent SituationCmdDraw / SituationCmdDrawIndexed.
pub fn situation_cmd_bind_vertex_buffer(cmd: SituationCommandBuffer, binding: u32, buffer: SituationBuffer, offset: usize, stride: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBindVertexBuffer(cmd, binding, buffer, offset, stride) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Blit between color 2D textures; caller owns explicit texture barriers.
pub fn situation_cmd_blit_texture(cmd: SituationCommandBuffer, src: SituationTexture, dst: SituationTexture, region: &mut SituationTextureBlitRegion) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBlitTexture(cmd, src, dst, region) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Record an explicit buffer-range memory barrier.
pub fn situation_cmd_buffer_barrier(cmd: SituationCommandBuffer, desc: &mut SituationBufferBarrierDesc) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdBufferBarrier(cmd, desc) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mid-pass clear of active render-pass attachments; begin-pass clears use SituationRenderPassInfo loadOp.
pub fn situation_cmd_clear(cmd: SituationCommandBuffer, clear_flags: u32, clear_value: &mut SituationClearValue) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdClear(cmd, clear_flags, clear_value) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mid-pass clear of the active color attachment.
pub fn situation_cmd_clear_color(cmd: SituationCommandBuffer, color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdClearColor(cmd, color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mid-pass clear of the active depth attachment.
pub fn situation_cmd_clear_depth(cmd: SituationCommandBuffer, depth: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdClearDepth(cmd, depth) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mid-pass clear of active depth and stencil attachments.
pub fn situation_cmd_clear_depth_stencil(cmd: SituationCommandBuffer, depth: f32, stencil: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdClearDepthStencil(cmd, depth, stencil) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mid-pass clear of the active stencil attachment when supported by backend/attachment state.
pub fn situation_cmd_clear_stencil(cmd: SituationCommandBuffer, stencil: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdClearStencil(cmd, stencil) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Legacy buffer-copy command (now returns error).
pub fn situation_cmd_copy_buffer(cmd: SituationCommandBuffer, src: SituationBuffer, dst: SituationBuffer, offset: usize, size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdCopyBuffer(cmd, src, dst, offset, size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Error-returning buffer-copy command with independent source/destination offsets.
pub fn situation_cmd_copy_buffer_ex(cmd: SituationCommandBuffer, src: SituationBuffer, dst: SituationBuffer, src_offset: usize, dst_offset: usize, size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdCopyBufferEx(cmd, src, dst, src_offset, dst_offset, size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Upload tightly packed RGBA8 rows from a buffer into a texture subregion; caller owns texture barriers.
pub fn situation_cmd_copy_buffer_to_texture(cmd: SituationCommandBuffer, src: SituationBuffer, src_offset: usize, dst: SituationTexture, dst_region: &mut SituationTextureCopyRegion) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdCopyBufferToTexture(cmd, src, src_offset, dst, dst_region) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Exact-size copy between color 2D textures; caller owns explicit texture barriers.
pub fn situation_cmd_copy_texture(cmd: SituationCommandBuffer, src: SituationTexture, dst: SituationTexture, region: &mut SituationTextureCopyRegion) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdCopyTexture(cmd, src, dst, region) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Copy a texture subregion into a buffer (`dst_row_pitch` 0 = width * 4); caller owns texture barriers.
pub fn situation_cmd_copy_texture_to_buffer(cmd: SituationCommandBuffer, src: SituationTexture, src_region: &mut SituationTextureCopyRegion, dst: SituationBuffer, dst_offset: usize, dst_row_pitch: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdCopyTextureToBuffer(cmd, src, src_region, dst, dst_offset, dst_row_pitch) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Record a command to dispatch compute shader work groups.
pub fn situation_cmd_dispatch(cmd: SituationCommandBuffer, group_count_x: u32, group_count_y: u32, group_count_z: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDispatch(cmd, group_count_x, group_count_y, group_count_z) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Record a compute dispatch with validation and error reporting.
pub fn situation_cmd_dispatch_ex(cmd: SituationCommandBuffer, group_count_x: u32, group_count_y: u32, group_count_z: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDispatchEx(cmd, group_count_x, group_count_y, group_count_z) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Record an indirect compute dispatch.
pub fn situation_cmd_dispatch_indirect(cmd: SituationCommandBuffer, indirect_buffer: SituationBuffer, offset: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDispatchIndirect(cmd, indirect_buffer, offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] [Core] Record a non-indexed draw call.
pub fn situation_cmd_draw(cmd: SituationCommandBuffer, vertex_count: u32, instance_count: u32, first_vertex: u32, first_instance: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDraw(cmd, vertex_count, instance_count, first_vertex, first_instance) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] [Core] Record an indexed draw call.
pub fn situation_cmd_draw_indexed(cmd: SituationCommandBuffer, index_count: u32, instance_count: u32, first_index: u32, vertex_offset: c_int, first_instance: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawIndexed(cmd, index_count, instance_count, first_index, vertex_offset, first_instance) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Indexed indirect draw (32-bit indices; requires bound index buffer). firstIndex is relative to SituationCmdBindIndexBuffer offset.
pub fn situation_cmd_draw_indexed_indirect(cmd: SituationCommandBuffer, indirect_buffer: SituationBuffer, offset: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawIndexedIndirect(cmd, indirect_buffer, offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Draw from a CPU/GPU-filled SituationDrawIndirectCommand in an indirect buffer (requires active render pass, bound pipeline, and vertex buffers).
pub fn situation_cmd_draw_indirect(cmd: SituationCommandBuffer, indirect_buffer: SituationBuffer, offset: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawIndirect(cmd, indirect_buffer, offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [High-Level] Records a command to draw a complete, pre-configured mesh.
pub fn situation_cmd_draw_mesh(cmd: SituationCommandBuffer, mesh: SituationMesh) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawMesh(cmd, mesh) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [High-Level] Record a command to draw a simple, colored 2D quad.
pub fn situation_cmd_draw_quad(cmd: SituationCommandBuffer, model: Mat4, color: Vector4) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawQuad(cmd, model, color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Draws a text string using GPU-accelerated textured quads.
pub fn situation_cmd_draw_text(cmd: SituationCommandBuffer, font: SituationFont, text: &CStr, pos: Vector2, color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawText(cmd, font, text.as_ptr(), pos, color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Text clipped to a rectangle with optional word wrap.
pub fn situation_cmd_draw_text_boxed(cmd: SituationCommandBuffer, font: SituationFont, text: &CStr, bounds: SitRectangle, fontSize: f32, spacing: f32, color: ColorRGBA, word_wrap: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawTextBoxed(cmd, font, text.as_ptr(), bounds, fontSize, spacing, color, word_wrap) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Advanced text drawing (scaling/spacing).
pub fn situation_cmd_draw_text_ex(cmd: SituationCommandBuffer, font: SituationFont, text: &CStr, pos: Vector2, fontSize: f32, spacing: f32, color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawTextEx(cmd, font, text.as_ptr(), pos, fontSize, spacing, color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [High-Level] Draw a part of a texture defined by a rectangle.
pub fn situation_cmd_draw_texture(cmd: SituationCommandBuffer, texture: SituationTexture, source: SitRectangle, dest: SitRectangle, origin: Vector2, rotation: f32, tint: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawTexture(cmd, texture, source, dest, origin, rotation, tint) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [High-Level] Draw texture with YPQ grade (matches SituationImageAdjustYPQ).
pub fn situation_cmd_draw_texture_ypq_grade(cmd: SituationCommandBuffer, texture: SituationTexture, source: SitRectangle, dest: SitRectangle, origin: Vector2, rotation: f32, phase_shift_deg: f32, chroma_factor: f32, luma_factor: f32, mix: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdDrawTextureYpqGrade(cmd, texture, source, dest, origin, rotation, phase_shift_deg, chroma_factor, luma_factor, mix) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_end_debug_group(cmd: SituationCommandBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdEndDebugGroup(cmd) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_end_render(cmd: SituationCommandBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdEndRender(cmd) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] Ends the current render pass.
pub fn situation_cmd_end_render_pass(cmd: SituationCommandBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdEndRenderPass(cmd) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Legacy convenience barrier; prefer SituationCmdPipelineBarrierEx, SituationCmdBufferBarrier, or SituationCmdTextureBarrier for new synchronization code.
pub fn situation_cmd_pipeline_barrier(cmd: SituationCommandBuffer, src_flags: u32, dst_flags: u32) {
    unsafe { SituationCmdPipelineBarrier(cmd, src_flags, dst_flags) };
}

/// Record an explicit global memory barrier.
pub fn situation_cmd_pipeline_barrier_ex(cmd: SituationCommandBuffer, desc: &mut SituationPipelineBarrierDesc) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdPipelineBarrierEx(cmd, desc) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_pop_raster_state(cmd: SituationCommandBuffer, scope_id: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdPopRasterState(cmd, scope_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Submits a command to copy a texture to the main window's swapchain (Compute-Only).
pub fn situation_cmd_present(cmd: SituationCommandBuffer, texture: SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdPresent(cmd, texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_push_raster_state(cmd: SituationCommandBuffer, scope_id: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdPushRasterState(cmd, scope_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_blend_enable(cmd: SituationCommandBuffer, enable: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetBlendEnable(cmd, enable) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_blend_func_separate(cmd: SituationCommandBuffer, src_rgb: SituationBlendFactor, dst_rgb: SituationBlendFactor, src_a: SituationBlendFactor, dst_a: SituationBlendFactor) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetBlendFuncSeparate(cmd, src_rgb, dst_rgb, src_a, dst_a) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_color_write_mask(cmd: SituationCommandBuffer, r: bool, g: bool, b: bool, a: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetColorWriteMask(cmd, r, g, b, a) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_cull_mode(cmd: SituationCommandBuffer, mode: SituationCullMode) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetCullMode(cmd, mode) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_depth_bias(cmd: SituationCommandBuffer, enable: bool, constant_factor: f32, clamp: f32, slope_factor: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetDepthBias(cmd, enable, constant_factor, clamp, slope_factor) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_depth_test(cmd: SituationCommandBuffer, enable: bool, depth_op: SituationDepthCompareOp) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetDepthTest(cmd, enable, depth_op) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_depth_write(cmd: SituationCommandBuffer, enable: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetDepthWrite(cmd, enable) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_front_face(cmd: SituationCommandBuffer, front_face: SituationFrontFace) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetFrontFace(cmd, front_face) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_line_width(cmd: SituationCommandBuffer, width: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetLineWidth(cmd, width) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_multisample_state(cmd: SituationCommandBuffer, state: *mut SituationMultisampleState) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetMultisampleState(cmd, state) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_polygon_mode(cmd: SituationCommandBuffer, mode: SituationPolygonMode) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetPolygonMode(cmd, mode) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_primitive_topology(cmd: SituationCommandBuffer, topology: SituationPrimitiveTopology) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetPrimitiveTopology(cmd, topology) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Core] Set a small block of per-draw uniform data (push constant).
pub fn situation_cmd_set_push_constant(cmd: SituationCommandBuffer, contract_id: u32, data: *mut c_void, size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetPushConstant(cmd, contract_id, data, size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_push_constant_data(cmd: SituationCommandBuffer, shader: SituationShader, offset: u32, data: *mut c_void, size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetPushConstantData(cmd, shader, offset, data, size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Sets the dynamic scissor rectangle to clip rendering.
pub fn situation_cmd_set_scissor(cmd: SituationCommandBuffer, x: c_int, y: c_int, width: c_int, height: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetScissor(cmd, x, y, width, height) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Sets scissor at index (0 = default scissor).
pub fn situation_cmd_set_scissor_indexed(cmd: SituationCommandBuffer, index: u32, x: c_int, y: c_int, width: c_int, height: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetScissorIndexed(cmd, index, x, y, width, height) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_cmd_set_stencil_test(cmd: SituationCommandBuffer, enable: bool, front: &mut SituationStencilState, back: &mut SituationStencilState) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetStencilTest(cmd, enable, front, back) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL Only, Deprecated v2.4] Attribute format + vertex buffer binding index. Prefer vertex pulling via SituationGetMeshVertexBufferAddress + sit/gpu/vertex_pull.glslh on Vulkan.
pub fn situation_cmd_set_vertex_attribute(cmd: SituationCommandBuffer, location: u32, binding: u32, size: c_int, r#type: SituationDataType, normalized: bool, offset: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetVertexAttribute(cmd, location, binding, size, r#type, normalized, offset) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Sets the dynamic viewport and scissor for the current render pass.
pub fn situation_cmd_set_viewport(cmd: SituationCommandBuffer, x: f32, y: f32, width: f32, height: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetViewport(cmd, x, y, width, height) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Sets viewport at index (0 = default viewport).
pub fn situation_cmd_set_viewport_indexed(cmd: SituationCommandBuffer, index: u32, x: f32, y: f32, width: f32, height: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdSetViewportIndexed(cmd, index, x, y, width, height) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Record an explicit texture layout/memory barrier.
pub fn situation_cmd_texture_barrier(cmd: SituationCommandBuffer, texture: SituationTexture, desc: &mut SituationTextureBarrierDesc) -> Result<(), SituationError> {
    let err = unsafe { SituationCmdTextureBarrier(cmd, texture, desc) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Converts a YPQA color back to the standard RGBA color space.
pub fn situation_color_from_ypq(ypq_color: ColorYPQA) -> ColorRGBA {
    unsafe { SituationColorFromYPQ(ypq_color) }
}

/// Float YPQ → RGBA (linear YIQ, clamped RGB).
pub fn situation_color_from_yp_qf(ypq: ColorYPQf) -> ColorRGBA {
    unsafe { SituationColorFromYPQf(ypq) }
}

/// sRGB 0–255 → PQ clear color as RGBA floats×255 for debugging.
pub fn situation_color_rgba_to_hdr_pq_clear(srgb: ColorRGBA) -> ColorRGBA {
    unsafe { SituationColorRgbaToHdrPqClear(srgb) }
}

/// Converts a standard RGBA color to the YPQA (Luma, Phase, Quadrature) color space.
pub fn situation_color_to_ypq(color: ColorRGBA) -> ColorYPQA {
    unsafe { SituationColorToYPQ(color) }
}

/// RGBA → normalized float YPQ (no 8-bit quantize).
pub fn situation_color_to_yp_qf(color: ColorRGBA) -> ColorYPQf {
    unsafe { SituationColorToYPQf(color) }
}

/// Configure a virtual display's properties.
pub fn situation_configure_virtual_display(display_id: c_int, offset: Vector2, opacity: f32, z_order: c_int, visible: bool, frame_time_mult: f64, blend_mode: SituationBlendMode) -> Result<(), SituationError> {
    let err = unsafe { SituationConfigureVirtualDisplay(display_id, offset, opacity, z_order, visible, frame_time_mult, blend_mode) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Eat a key press this frame so later IsKeyPressed() returns false (e.g. for global hotkeys to take priority over app controls).
pub fn situation_consume_key_press(key: c_int) {
    unsafe { SituationConsumeKeyPress(key) };
}

/// Convert an 8-bit ColorRGBA struct to a normalized Vector4.
pub fn situation_convert_color_to_vector4(c: ColorRGBA, out_normalized_color: &mut Vector4) {
    unsafe { SituationConvertColorToVector4(c, out_normalized_color) };
}

/// Copy a file.
pub fn situation_copy_file(source_path: &CStr, dest_path: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationCopyFile(source_path.as_ptr(), dest_path.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_ascii_font(data: &mut u8, cw: c_int, ch: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateASCIIFont(data, cw, ch, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a generic GPU data buffer (e.g., SSBO).
pub fn situation_create_buffer(size: usize, initial_data: *mut c_void, usage_flags: SituationBufferUsageFlags, out_buffer: &mut SituationBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateBuffer(size, initial_data, usage_flags, out_buffer) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_cp437_font(font_data_8x16: &mut u8, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateCP437Font(font_data_8x16, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a compute pipeline from a shader file.
pub fn situation_create_compute_pipeline(compute_shader_path: &CStr, layout_type: SituationComputeLayoutType, out_pipeline: &mut SituationComputePipeline) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateComputePipeline(compute_shader_path.as_ptr(), layout_type, out_pipeline) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a compute pipeline from in-memory GLSL source.
pub fn situation_create_compute_pipeline_from_memory(compute_shader_source: &CStr, layout_type: SituationComputeLayoutType, out_pipeline: &mut SituationComputePipeline) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateComputePipelineFromMemory(compute_shader_source.as_ptr(), layout_type, out_pipeline) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a directory, optionally creating parent directories.
pub fn situation_create_directory(dir_path: &CStr, create_parents: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateDirectory(dir_path.as_ptr(), create_parents) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a new audio processing graph.
pub fn situation_create_graph() -> Option<*mut SituationAudioGraph> {
    let ptr = unsafe { SituationCreateGraph() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Allocates a new SituationImage container with UNINITIALIZED data.
pub fn situation_create_image(width: c_int, height: c_int, channels: c_int, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateImage(width, height, channels, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a mesh from vertex and index data.
pub fn situation_create_mesh(vertex_data: *mut c_void, vertex_count: c_int, vertex_stride: usize, index_data: &mut u32, index_count: c_int, out_mesh: &mut SituationMesh) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateMesh(vertex_data, vertex_count, vertex_stride, index_data, index_count, out_mesh) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a mesh with an explicit vertex layout tag (includes SIT_MESH_LAYOUT_PULL).
pub fn situation_create_mesh_ex(vertex_data: *mut c_void, vertex_count: c_int, vertex_stride: usize, index_data: &mut u32, index_count: c_int, layout: SituationMeshVertexLayout, out_mesh: &mut SituationMesh) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateMeshEx(vertex_data, vertex_count, vertex_stride, index_data, index_count, layout, out_mesh) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a node of the given type in the graph.
pub fn situation_create_node(graph: *mut SituationAudioGraph, r#type: SituationNodeType, handle: &mut SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateNode(graph, r#type, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_outlined_packed_bitmap_font(packed_data: *mut c_void, config: &mut SituationPackedFont, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateOutlinedPackedBitmapFont(packed_data, config, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_packed_bitmap_font(packed_data: *mut c_void, config: &mut SituationPackedFont, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreatePackedBitmapFont(packed_data, config, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Connect an output port to an input port.
pub fn situation_create_patch(graph: *mut SituationAudioGraph, src: SituationNodeHandle, src_port: c_int, dst: SituationNodeHandle, dst_port: c_int, is_control: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCreatePatch(graph, src, src_port, dst, dst_port, is_control) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Phase 1] Create an async GPU->CPU staging buffer.
pub fn situation_create_readback_buffer(size: usize, out_buffer: &mut SituationBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateReadbackBuffer(size, out_buffer) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_terminal_font_ex(data: &mut u8, char_width: c_int, char_height: c_int, char_count: c_int, chars_per_row: c_int, first_char: c_int, char_spacing: f32, line_spacing: f32, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateTerminalFontEx(data, char_width, char_height, char_count, chars_per_row, first_char, char_spacing, line_spacing, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_terminal_font_from_memory(data: &mut u8, char_width: c_int, char_height: c_int, char_count: c_int, chars_per_row: c_int, first_char: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateTerminalFontFromMemory(data, char_width, char_height, char_count, chars_per_row, first_char, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a texture from a CPU-side image.
pub fn situation_create_texture(image: SituationImage, generate_mipmaps: bool, out_texture: &mut SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateTexture(image, generate_mipmaps, out_texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a texture with specific usage flags.
pub fn situation_create_texture_ex(image: SituationImage, generate_mipmaps: bool, flags: SituationTextureUsageFlags, out_texture: &mut SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateTextureEx(image, generate_mipmaps, flags, out_texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Initializes the thread pool with dual-priority queues and worker threads.
pub fn situation_create_thread_pool(pool: *mut SituationThreadPool, num_threads: usize, queue_size: usize, hot_reload_rate: f64, disable_io: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateThreadPool(pool, num_threads, queue_size, hot_reload_rate, disable_io) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_vcr_font(font_data: &mut u16, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVCRFont(font_data, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_vcr_font_with_outline(data: &mut u16, outline_thickness: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVCRFontWithOutline(data, outline_thickness, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_vga8x8_font(data: &mut u8, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVGA8x8Font(data, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_create_vga8x8_font_with_outline(data: &mut u8, outline_thickness: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVGA8x8FontWithOutline(data, outline_thickness, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create an off-screen render target.
pub fn situation_create_virtual_display(resolution: Vector2, frame_time_mult: f64, z_order: c_int, scaling_mode: SituationScalingMode, blend_mode: SituationBlendMode, out_id: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVirtualDisplay(resolution, frame_time_mult, z_order, scaling_mode, blend_mode, out_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a virtual display with extended flags (e.g. COMPUTE_TARGET for compute shader writes).
pub fn situation_create_virtual_display_ex(resolution: Vector2, frame_time_mult: f64, z_order: c_int, scaling_mode: SituationScalingMode, blend_mode: SituationBlendMode, flags: SituationVDFlags, out_id: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVirtualDisplayEx(resolution, frame_time_mult, z_order, scaling_mode, blend_mode, flags, out_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a virtual display from a full desc (VD-1 attachment config).
pub fn situation_create_virtual_display_from_desc(desc: &mut SituationVirtualDisplayDesc, out_id: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationCreateVirtualDisplayFromDesc(desc, out_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Delete a directory, optionally deleting all its contents.
pub fn situation_delete_directory(dir_path: &CStr, recursive: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationDeleteDirectory(dir_path.as_ptr(), recursive) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Delete a file.
pub fn situation_delete_file(file_path: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationDeleteFile(file_path.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Deserialize a graph from a JSON string.
pub fn situation_deserialize_graph_from_json(graph: *mut SituationAudioGraph, json_string: &CStr, device_funcs: &mut SituationDeviceFunctions, num_device_funcs: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationDeserializeGraphFromJSON(graph, json_string.as_ptr(), device_funcs, num_device_funcs) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Destroy a GPU buffer.
pub fn situation_destroy_buffer(buffer: &mut SituationBuffer) {
    unsafe { SituationDestroyBuffer(buffer) };
}

/// Destroy a compute pipeline and free its GPU resources.
pub fn situation_destroy_compute_pipeline(pipeline: &mut SituationComputePipeline) {
    unsafe { SituationDestroyComputePipeline(pipeline) };
}

/// Destroy a graph and all its nodes/patches.
pub fn situation_destroy_graph(graph: *mut SituationAudioGraph) {
    unsafe { SituationDestroyGraph(graph) };
}

/// Unload a mesh from GPU memory.
pub fn situation_destroy_mesh(mesh: &mut SituationMesh) {
    unsafe { SituationDestroyMesh(mesh) };
}

/// Remove and destroy a node from the graph.
pub fn situation_destroy_node(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationDestroyNode(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Disconnect a patch between two ports (legacy, no is_control param).
pub fn situation_destroy_patch(graph: *mut SituationAudioGraph, src: SituationNodeHandle, src_port: c_int, dst: SituationNodeHandle, dst_port: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationDestroyPatch(graph, src, src_port, dst, dst_port) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Unload a texture from GPU memory.
pub fn situation_destroy_texture(texture: &mut SituationTexture) {
    unsafe { SituationDestroyTexture(texture) };
}

/// Shuts down the thread pool and releases resources.
pub fn situation_destroy_thread_pool(pool: *mut SituationThreadPool) {
    unsafe { SituationDestroyThreadPool(pool) };
}

/// Destroy a virtual display.
pub fn situation_destroy_virtual_display(display_id: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationDestroyVirtualDisplay(display_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Detach a custom DSP processor from a sound.
pub fn situation_detach_audio_processor(sound: &mut SituationSound, processor: SituationAudioProcessorCallback, user_data: *mut c_void) -> Result<(), SituationError> {
    let err = unsafe { SituationDetachAudioProcessor(sound, processor, user_data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Check if a directory exists at the given path.
pub fn situation_directory_exists(dir_path: &CStr) -> bool {
    unsafe { SituationDirectoryExists(dir_path.as_ptr()) }
}

/// Hide and lock the cursor, providing raw mouse motion.
pub fn situation_disable_cursor() {
    unsafe { SituationDisableCursor() };
}

/// Disable MIDI control for a node.
pub fn situation_disable_midi_control(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationDisableMidiControl(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Disable MIDI Learn for a node.
pub fn situation_disable_midi_learn(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationDisableMidiLearn(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Executes a loop in parallel across worker threads (Fork-Join).
pub fn situation_dispatch_parallel(pool: *mut SituationThreadPool, count: c_int, min_batch_size: c_int, user_data: *mut c_void) {
    unsafe { SituationDispatchParallel(pool, count, min_batch_size, user_data) };
}

/// Draws FPS, Latency, and Memory stats
pub fn situation_draw_metrics_overlay(cmd: SituationCommandBuffer, position: Vector2, color: ColorRGBA) {
    unsafe { SituationDrawMetricsOverlay(cmd, position, color) };
}

/// Draws all sub-meshes of a model with a single root transformation.
pub fn situation_draw_model(cmd: SituationCommandBuffer, model: SituationModel, transform: Mat4) -> Result<(), SituationError> {
    let err = unsafe { SituationDrawModel(cmd, model, transform) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Prints the current task graph state to the stream.
pub fn situation_dump_task_graph(pool: *mut SituationThreadPool, out_stream: &mut FILE, json_mode: bool) {
    unsafe { SituationDumpTaskGraph(pool, out_stream, json_mode) };
}

/// Metrics-only dump
pub fn situation_dump_thread_pool_metrics(pool: *mut SituationThreadPool, out_stream: &mut FILE, json_mode: bool) {
    unsafe { SituationDumpThreadPoolMetrics(pool, out_stream, json_mode) };
}

/// Pool metrics + per-role CPU snapshot
pub fn situation_dump_thread_pool_status(pool: *mut SituationThreadPool, out_stream: &mut FILE, json_mode: bool) {
    unsafe { SituationDumpThreadPoolStatus(pool, out_stream, json_mode) };
}

/// Status + topology line + pool dump
pub fn situation_dump_threading_report(pool: *mut SituationThreadPool, out_stream: &mut FILE, json_mode: bool) {
    unsafe { SituationDumpThreadingReport(pool, out_stream, json_mode) };
}

/// Enable MIDI CC control for a node. Pass device_id=-1 for auto-select.
pub fn situation_enable_midi_control(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, device_id: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationEnableMidiControl(graph, handle, device_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Enable MIDI Learn capability for a node. MIDI must already be enabled.
pub fn situation_enable_midi_learn(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationEnableMidiLearn(graph, handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [GL+VK] Submit all commands for the frame and present the result.
pub fn situation_end_frame() -> Result<(), SituationError> {
    let err = unsafe { SituationEndFrame() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Caller frees via SituationFreeDeviceList] Canonical device enumeration.
pub fn situation_enumerate_audio_devices(out_count: &mut c_int) -> Option<*mut SituationAudioDeviceInfo> {
    let ptr = unsafe { SituationEnumerateAudioDevices(out_count) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Human-readable base label for an error code (from the errno table).
pub fn situation_error_to_string(err: SituationError) -> Option<*const c_char> {
    let ptr = unsafe { SituationErrorToString(err) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Execute a shell command hidden, return exit code & combined output.
pub fn situation_execute_command() -> c_int {
    unsafe { SituationExecuteCommand() }
}

/// Export image data (.png, .bmp, .jpg, .tga, .hdr).
pub fn situation_export_image(image: SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationExportImage(image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Write a text-based frame time histogram into buf.
pub fn situation_export_render_histogram(buf: &CStr, buf_size: usize) {
    unsafe { SituationExportRenderHistogram(buf.as_ptr(), buf_size) };
}

/// Check if a file exists at the given path.
pub fn situation_file_exists(file_path: &CStr) -> bool {
    unsafe { SituationFileExists(file_path.as_ptr()) }
}

/// Find the best matching device by type and channel requirements.
pub fn situation_find_best_device(preferred_type: SituationAudioDeviceType, min_channels_out: u32, min_channels_in: u32) -> Option<*mut SituationAudioDeviceInfo> {
    let ptr = unsafe { SituationFindBestDevice(preferred_type, min_channels_out, min_channels_in) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Free a device list returned by SituationEnumerateAudioDevices.
pub fn situation_free_device_list(devices: &mut SituationAudioDeviceInfo, count: c_int) {
    unsafe { SituationFreeDeviceList(devices, count) };
}

/// Free the memory allocated by SituationListDirectoryFiles.
pub fn situation_free_directory_file_list(file_list: *const *const c_char, count: c_int) {
    unsafe { SituationFreeDirectoryFileList(file_list, count) };
}

/// Free a display info array returned by SituationGetDisplays.
pub fn situation_free_displays(displays: &mut SituationDisplayInfo, count: c_int) {
    unsafe { SituationFreeDisplays(displays, count) };
}

/// Free a JSON string returned by SituationSerializeGraphToJSON.
pub fn situation_free_json_string(json_string: &CStr) {
    unsafe { SituationFreeJSONString(json_string.as_ptr()) };
}

/// Free a process list returned by SituationGetProcessList().
pub fn situation_free_process_list(list: &mut SituationProcessInfo, count: c_int) {
    unsafe { SituationFreeProcessList(list, count) };
}

/// Free a string allocated by the library (e.g., from path helpers).
pub fn situation_free_string(str: &CStr) {
    unsafe { SituationFreeString(str.as_ptr()) };
}

/// Generate a new image of a solid color.
pub fn situation_gen_image_color(width: c_int, height: c_int, color: ColorRGBA, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationGenImageColor(width, height, color, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Generate a new image with a gradient.
pub fn situation_gen_image_gradient(width: c_int, height: c_int, tl: ColorRGBA, tr: ColorRGBA, bl: ColorRGBA, br: ColorRGBA, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationGenImageGradient(width, height, tl, tr, bl, br, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get the name of the currently active playback device (static buffer, do not free).
pub fn situation_get_active_audio_device_name() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetActiveAudioDeviceName() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the currently active audio processing graph (NULL if none).
pub fn situation_get_active_graph() -> Option<*mut SituationAudioGraph> {
    let ptr = unsafe { SituationGetActiveGraph() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// active_jobs counter
pub fn situation_get_active_job_count(pool: *mut SituationThreadPool) -> c_int {
    unsafe { SituationGetActiveJobCount(pool) }
}

/// Get a safe, persistent path for saving application data (caller must free).
pub fn situation_get_app_save_path(app_name: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetAppSavePath(app_name.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the value of an argument (e.g., "jungle" from "-level:jungle").
pub fn situation_get_argument_value(arg_name: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetArgumentValue(arg_name.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

pub fn situation_get_audio_devices(count: &mut c_int) -> Option<*mut SituationAudioDeviceInfo> {
    let ptr = unsafe { SituationGetAudioDevices(count) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the master volume for the audio device.
pub fn situation_get_audio_master_volume() -> f32 {
    unsafe { SituationGetAudioMasterVolume() }
}

/// Get the sample rate of the current audio device.
pub fn situation_get_audio_playback_sample_rate() -> c_int {
    unsafe { SituationGetAudioPlaybackSampleRate() }
}

/// Get the path to the directory containing the executable (caller must free).
pub fn situation_get_base_path() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetBasePath() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Read data from a GPU buffer (blocking).
pub fn situation_get_buffer_data(buffer: SituationBuffer, offset: usize, size: usize, out_data: *mut c_void) -> Result<(), SituationError> {
    let err = unsafe { SituationGetBufferData(buffer, offset, size, out_data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Retrieves the GPU device address of a buffer for bindless access.
pub fn situation_get_buffer_device_address(buffer: SituationBuffer) -> u64 {
    unsafe { SituationGetBufferDeviceAddress(buffer) }
}

/// Gets physical processors (Cores) from cached topology
pub fn situation_get_cpu_core_count() -> u32 {
    unsafe { SituationGetCPUCoreCount() }
}

/// CPU name, core/thread counts, and clock speed.
pub fn situation_get_cpu_info(out: &mut SituationCPUInfo) {
    unsafe { SituationGetCPUInfo(out) };
}

/// Logical processor count (cached topology).
pub fn situation_get_cpu_thread_count() -> u32 {
    unsafe { SituationGetCPUThreadCount() }
}

/// Get the display name for a device category.
pub fn situation_get_category_name(category: SituationDeviceCategory) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetCategoryName(category) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Maps a physical key scancode (plus modifiers) to a Unicode character, respecting the current OS keyboard layout.
pub fn situation_get_char_from_scancode(window: c_int, scancode: c_int, mods: c_int, out_char: &mut u32) -> c_int {
    unsafe { SituationGetCharFromScancode(window, scancode, mods, out_char) }
}

/// Get the next character from the text input queue.
pub fn situation_get_char_pressed() -> c_uint {
    unsafe { SituationGetCharPressed() }
}

/// Get text from the system clipboard.
pub fn situation_get_clipboard_text(out_text: *const *const c_char) -> Result<(), SituationError> {
    let err = unsafe { SituationGetClipboardText(out_text) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [VK] Get the compute-specific command buffer (Vulkan only).
pub fn situation_get_compute_command_buffer() -> Option<SituationCommandBuffer> {
    let ptr = unsafe { SituationGetComputeCommandBuffer() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Effective audio mask (init or default)
pub fn situation_get_configured_audio_thread_affinity() -> u64 {
    unsafe { SituationGetConfiguredAudioThreadAffinity() }
}

/// Effective I/O mask (init or default CPU 3)
pub fn situation_get_configured_io_thread_affinity() -> u64 {
    unsafe { SituationGetConfiguredIOThreadAffinity() }
}

/// Init mask for main thread (0 = no pin)
pub fn situation_get_configured_main_thread_affinity() -> u64 {
    unsafe { SituationGetConfiguredMainThreadAffinity() }
}

/// Effective render mask (init or default)
pub fn situation_get_configured_render_thread_affinity() -> u64 {
    unsafe { SituationGetConfiguredRenderThreadAffinity() }
}

/// Get the current value of a node's control parameter.
pub fn situation_get_control(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, control_id: u32, out_value: &mut f32) -> Result<(), SituationError> {
    let err = unsafe { SituationGetControl(graph, handle, control_id, out_value) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Pointer to cached topology (NULL on failure)
pub fn situation_get_cpu_topology(out_topology: &mut SituationCpuTopology) -> Result<(), SituationError> {
    let err = unsafe { SituationGetCpuTopology(out_topology) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Gets flags based on current GLFW window state
pub fn situation_get_current_actual_window_state_flags() -> u32 {
    unsafe { SituationGetCurrentActualWindowStateFlags() }
}

/// Get the drive letter of the running executable (Windows only).
pub fn situation_get_current_drive_letter() -> c_char {
    unsafe { SituationGetCurrentDriveLetter() }
}

/// Get the index of the monitor the window is on.
pub fn situation_get_current_monitor() -> c_int {
    unsafe { SituationGetCurrentMonitor() }
}

/// Logical CPU index for current thread, or -1 if unknown
pub fn situation_get_current_processor_index() -> c_int {
    unsafe { SituationGetCurrentProcessorIndex() }
}

pub fn situation_get_device_info() -> SituationDeviceInfo {
    unsafe { SituationGetDeviceInfo() }
}

/// Get metadata for a registered device type.
pub fn situation_get_device_metadata(r#type: SituationNodeType, out_meta: &mut SituationDeviceMetadata) -> Result<(), SituationError> {
    let err = unsafe { SituationGetDeviceMetadata(r#type, out_meta) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get information for all displays (caller must free).
pub fn situation_get_displays(out_displays: &mut SituationDisplayInfo, out_count: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationGetDisplays(out_displays, out_count) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Number of draw commands this frame
pub fn situation_get_draw_call_count() -> u32 {
    unsafe { SituationGetDrawCallCount() }
}

/// Get info for a specific drive (Windows only).
pub fn situation_get_drive_info(drive_letter: c_char, out_total_capacity_bytes: &mut u64, out_free_space_bytes: &mut u64, out_volume_name: &CStr, volume_name_len: c_int) -> bool {
    unsafe { SituationGetDriveInfo(drive_letter, out_total_capacity_bytes, out_free_space_bytes, out_volume_name.as_ptr(), volume_name_len) }
}

/// Get the current frames-per-second value.
pub fn situation_get_fps() -> c_int {
    unsafe { SituationGetFPS() }
}

/// Extract the file extension from a path.
pub fn situation_get_file_extension(file_path: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetFileExtension(file_path.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the last modification time of a file (Unix timestamp).
pub fn situation_get_file_mod_time(file_path: &CStr) -> c_long {
    unsafe { SituationGetFileModTime(file_path.as_ptr()) }
}

/// Extract the file name (including extension) from a full path.
pub fn situation_get_file_name(full_path: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetFileName(full_path.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Count of detected frame spikes (general debugging aid)
pub fn situation_get_frame_spike_count() -> u32 {
    unsafe { SituationGetFrameSpikeCount() }
}

/// Get the time in seconds for the last frame to complete (deltaTime).
pub fn situation_get_frame_time() -> f32 {
    unsafe { SituationGetFrameTime() }
}

/// Get the raw GLFW window handle.
pub fn situation_get_glf_wwindow() -> Option<*mut GLFWwindow> {
    let ptr = unsafe { SituationGetGLFWwindow() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// GPU name and dedicated VRAM (when available).
pub fn situation_get_gpu_info(out: &mut SituationGPUInfo) {
    unsafe { SituationGetGPUInfo(out) };
}

/// Get the name of the active GPU.
pub fn situation_get_gpu_name() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetGPUName() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the number of axes for a gamepad.
pub fn situation_get_gamepad_axis_count(jid: c_int) -> c_int {
    unsafe { SituationGetGamepadAxisCount(jid) }
}

/// Get the value of a gamepad axis (deadzone applied).
pub fn situation_get_gamepad_axis_value(jid: c_int, axis: c_int) -> f32 {
    unsafe { SituationGetGamepadAxisValue(jid, axis) }
}

/// Get the next gamepad button from the press queue.
pub fn situation_get_gamepad_button_pressed() -> c_int {
    unsafe { SituationGetGamepadButtonPressed() }
}

pub fn situation_get_graphics_backend() -> SituationGraphicsBackend {
    unsafe { SituationGetGraphicsBackend() }
}

pub fn situation_get_graphics_backend_name() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetGraphicsBackendName() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get backend capabilities for examples/frameworks.
pub fn situation_get_graphics_caps(out_caps: &mut SituationGraphicsCaps) {
    unsafe { SituationGetGraphicsCaps(out_caps) };
}

/// High-priority queue depth
pub fn situation_get_high_queue_depth(pool: *mut SituationThreadPool) -> usize {
    unsafe { SituationGetHighQueueDepth(pool) }
}

/// [v2.3.37] Get the current depth of the IO/Low Priority queue
pub fn situation_get_io_queue_depth() -> usize {
    unsafe { SituationGetIOQueueDepth() }
}

/// Query the current initialization state (thread-safe).
pub fn situation_get_init_state() -> SituationInitState {
    unsafe { SituationGetInitState() }
}

/// Number of input devices (keyboard/mouse/gamepad).
pub fn situation_get_input_device_count() -> c_int {
    unsafe { SituationGetInputDeviceCount() }
}

pub fn situation_get_input_device_name(index: c_int, out_name: &CStr, name_len: c_int) -> bool {
    unsafe { SituationGetInputDeviceName(index, out_name.as_ptr(), name_len) }
}

/// Returns pointer to the library's internal thread pool (NULL if not initialized).
pub fn situation_get_internal_thread_pool() -> Option<*mut SituationThreadPool> {
    let ptr = unsafe { SituationGetInternalThreadPool() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the human-readable name of a joystick/gamepad.
pub fn situation_get_joystick_name(jid: c_int) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetJoystickName(jid) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the next key from the press queue (no repeats).
pub fn situation_get_key_pressed() -> c_int {
    unsafe { SituationGetKeyPressed() }
}

/// Get the next key and its scancode from the queue.
pub fn situation_get_key_pressed_ex(out_scancode: &mut c_int) -> c_int {
    unsafe { SituationGetKeyPressedEx(out_scancode) }
}

/// Get the platform-specific scancode for a logical key.
pub fn situation_get_key_scancode(key: c_int) -> c_int {
    unsafe { SituationGetKeyScancode(key) }
}

/// Get the SituationError enum from the most recent _SituationSetErrorFromCode call.
pub fn situation_get_last_error_code() -> Result<(), SituationError> {
    let err = unsafe { SituationGetLastErrorCode() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get the last error message as a string (caller must free).
pub fn situation_get_last_error_msg(out_msg: *const *const c_char) -> Result<(), SituationError> {
    let err = unsafe { SituationGetLastErrorMsg(out_msg) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_get_last_frame_phases(backpressure_ns: &mut u64, fence_wait_ns: &mut u64, execute_ns: &mut u64, present_ns: &mut u64) {
    unsafe { SituationGetLastFramePhases(backpressure_ns, fence_wait_ns, execute_ns, present_ns) };
}

/// Get the time taken for the last virtual display composite pass.
pub fn situation_get_last_vd_composite_time_ms() -> f64 {
    unsafe { SituationGetLastVDCompositeTimeMS() }
}

/// [GL+VK] Get the primary command buffer for the current frame.
pub fn situation_get_main_command_buffer() -> Option<SituationCommandBuffer> {
    let ptr = unsafe { SituationGetMainCommandBuffer() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Last playback callback block: peak sample magnitude & RMS (optional pointers; safe from main/UI thread).
pub fn situation_get_master_output_meter(out_peak: &mut f32, out_rms: &mut f32) {
    unsafe { SituationGetMasterOutputMeter(out_peak, out_rms) };
}

/// Query maximum compute work group count per dispatch.
pub fn situation_get_max_compute_work_groups(x: &mut u32, y: &mut u32, z: &mut u32) {
    unsafe { SituationGetMaxComputeWorkGroups(x, y, z) };
}

/// Highest observed frame delta (general spike debugging)
pub fn situation_get_max_frame_time() -> f64 {
    unsafe { SituationGetMaxFrameTime() }
}

/// Total and available physical RAM.
pub fn situation_get_memory_info(out: &mut SituationMemoryInfo) {
    unsafe { SituationGetMemoryInfo(out) };
}

/// Get raw vertex/index data pointers from a mesh (read-only).
pub fn situation_get_mesh_data(mesh: SituationMesh, vertex_data: *mut c_void, vertex_count: &mut c_int, vertex_stride: &mut c_int, index_data: *mut c_void, index_count: &mut c_int) {
    unsafe { SituationGetMeshData(mesh, vertex_data, vertex_count, vertex_stride, index_data, index_count) };
}

/// Retrieves the GPU device address of the mesh index buffer. [VK] requires SIT_FEATURE_BINDLESS_BUFFERS; [GL] NVIDIA-only via NV_shader_buffer_load. Returns 0 if unsupported.
pub fn situation_get_mesh_index_buffer_address(mesh: SituationMesh) -> u64 {
    unsafe { SituationGetMeshIndexBufferAddress(mesh) }
}

/// GPU VA of mesh vertex buffer. [VK] SIT_FEATURE_BINDLESS_BUFFERS; pull draw: SituationCmdBindMeshPullBuffers + buffer_reference VS (see sit/gpu/vertex_pull.glslh).
pub fn situation_get_mesh_vertex_buffer_address(mesh: SituationMesh) -> u64 {
    unsafe { SituationGetMeshVertexBufferAddress(mesh) }
}

/// Query layout tag stored at creation.
pub fn situation_get_mesh_vertex_layout(mesh: SituationMesh, out_layout: &mut SituationMeshVertexLayout) -> Result<(), SituationError> {
    let err = unsafe { SituationGetMeshVertexLayout(mesh, out_layout) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// PortMidi device name for device_id (hardware or virtual).
pub fn situation_get_midi_device_name(device_id: c_int, out_name: &CStr, out_name_size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationGetMidiDeviceName(device_id, out_name.as_ptr(), out_name_size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get the number of connected monitors.
pub fn situation_get_monitor_count() -> c_int {
    unsafe { SituationGetMonitorCount() }
}

/// Get the height of a monitor's current video mode.
pub fn situation_get_monitor_height(monitor_id: c_int) -> c_int {
    unsafe { SituationGetMonitorHeight(monitor_id) }
}

/// Get the human-readable name of a monitor.
pub fn situation_get_monitor_name(monitor_id: c_int) -> Option<*const c_char> {
    let ptr = unsafe { SituationGetMonitorName(monitor_id) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the physical height of a monitor in millimeters.
pub fn situation_get_monitor_physical_height(monitor_id: c_int) -> c_int {
    unsafe { SituationGetMonitorPhysicalHeight(monitor_id) }
}

/// Get the physical width of a monitor in millimeters.
pub fn situation_get_monitor_physical_width(monitor_id: c_int) -> c_int {
    unsafe { SituationGetMonitorPhysicalWidth(monitor_id) }
}

/// Get the top-left position of a monitor on the desktop.
pub fn situation_get_monitor_position(monitor_id: c_int) -> Vector2 {
    unsafe { SituationGetMonitorPosition(monitor_id) }
}

/// Get the refresh rate of a monitor.
pub fn situation_get_monitor_refresh_rate(monitor_id: c_int) -> c_int {
    unsafe { SituationGetMonitorRefreshRate(monitor_id) }
}

/// Get the width of a monitor's current video mode.
pub fn situation_get_monitor_width(monitor_id: c_int) -> c_int {
    unsafe { SituationGetMonitorWidth(monitor_id) }
}

/// Get the mouse movement since the last frame.
pub fn situation_get_mouse_delta() -> Vector2 {
    unsafe { SituationGetMouseDelta() }
}

/// Get the mouse position within the window.
pub fn situation_get_mouse_position() -> Vector2 {
    unsafe { SituationGetMousePosition() }
}

/// Get vertical mouse wheel movement.
pub fn situation_get_mouse_wheel_move() -> f32 {
    unsafe { SituationGetMouseWheelMove() }
}

/// Get vertical and horizontal mouse wheel movement.
pub fn situation_get_mouse_wheel_move_v() -> Vector2 {
    unsafe { SituationGetMouseWheelMoveV() }
}

/// Number of network adapters reported by the OS.
pub fn situation_get_network_adapter_count() -> c_int {
    unsafe { SituationGetNetworkAdapterCount() }
}

pub fn situation_get_network_adapter_name(index: c_int, out_name: &CStr, name_len: c_int) -> bool {
    unsafe { SituationGetNetworkAdapterName(index, out_name.as_ptr(), name_len) }
}

/// Get a direct pointer to a node (for advanced use).
pub fn situation_get_node(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> Option<*mut SituationNode> {
    let ptr = unsafe { SituationGetNode(graph, handle) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Query how many frames of space are available in the PCM_INPUT node's ring buffer.
pub fn situation_get_node_pcm_free_frames(graph: *mut SituationAudioGraph, node: SituationNodeHandle) -> u32 {
    unsafe { SituationGetNodePCMFreeFrames(graph, node) }
}

/// Cached NUMA snapshot
pub fn situation_get_numa_topology(out_topology: &mut SituationNumaTopology) -> Result<(), SituationError> {
    let err = unsafe { SituationGetNumaTopology(out_topology) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get operating system name, version, and build number.
pub fn situation_get_os_info() -> SituationOSInfo {
    unsafe { SituationGetOSInfo() }
}

/// TLS: node for current thread, or -1 if unset
pub fn situation_get_preferred_numa_node() -> c_int {
    unsafe { SituationGetPreferredNumaNode() }
}

/// Get snapshot of running OS processes. Caller must free with SituationFreeProcessList().
pub fn situation_get_process_list(out_count: &mut c_int) -> Option<*mut SituationProcessInfo> {
    let ptr = unsafe { SituationGetProcessList(out_count) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Pending jobs per queue mask
pub fn situation_get_queue_depth(pool: *mut SituationThreadPool, mask: SituationJobQueueMask) -> usize {
    unsafe { SituationGetQueueDepth(pool, mask) }
}

/// Sizing helper (no pool required)
pub fn situation_get_recommended_worker_count(reserved_threads: u32, use_physical_cores: bool) -> u32 {
    unsafe { SituationGetRecommendedWorkerCount(reserved_threads, use_physical_cores) }
}

/// Get the number of registered audio device types.
pub fn situation_get_registered_device_count() -> c_int {
    unsafe { SituationGetRegisteredDeviceCount() }
}

/// Get the current render height (backbuffer size, considers HiDPI).
pub fn situation_get_render_height() -> c_int {
    unsafe { SituationGetRenderHeight() }
}

/// Get render thread latency metrics
pub fn situation_get_render_latency_stats(avg_ns: &mut u64, max_ns: &mut u64) {
    unsafe { SituationGetRenderLatencyStats(avg_ns, max_ns) };
}

/// Get the current depth of the render queue
pub fn situation_get_render_queue_depth() -> usize {
    unsafe { SituationGetRenderQueueDepth() }
}

/// Get the current render width (backbuffer size, considers HiDPI).
pub fn situation_get_render_width() -> c_int {
    unsafe { SituationGetRenderWidth() }
}

/// Legacy — prefer SituationGetGraphicsBackend() + SituationGetGraphicsCaps().
pub fn situation_get_renderer_type() -> SituationRendererType {
    unsafe { SituationGetRendererType() }
}

/// Get the current logical height of the window.
pub fn situation_get_screen_height() -> c_int {
    unsafe { SituationGetScreenHeight() }
}

/// Get the current logical width of the window.
pub fn situation_get_screen_width() -> c_int {
    unsafe { SituationGetScreenWidth() }
}

/// Get the current default screenshot format.
pub fn situation_get_screenshot_format() -> SituationScreenshotFormat {
    unsafe { SituationGetScreenshotFormat() }
}

/// Get the current serialization format version string.
pub fn situation_get_serialization_version() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetSerializationVersion() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get the stereo pan of a sound.
pub fn situation_get_sound_pan(sound: &mut SituationSound) -> f32 {
    unsafe { SituationGetSoundPan(sound) }
}

/// Get the pitch of a sound.
pub fn situation_get_sound_pitch(sound: &mut SituationSound) -> f32 {
    unsafe { SituationGetSoundPitch(sound) }
}

/// Get the volume of a specific sound.
pub fn situation_get_sound_volume(sound: &mut SituationSound) -> f32 {
    unsafe { SituationGetSoundVolume(sound) }
}

pub fn situation_get_storage_device(index: c_int, out_name: &CStr, name_len: c_int, out_capacity_bytes: &mut u64, out_free_bytes: &mut u64) -> bool {
    unsafe { SituationGetStorageDevice(index, out_name.as_ptr(), name_len, out_capacity_bytes, out_free_bytes) }
}

/// Number of storage volumes reported by the OS.
pub fn situation_get_storage_device_count() -> c_int {
    unsafe { SituationGetStorageDeviceCount() }
}

/// Lines required for width constraint (grid/TTF).
pub fn situation_get_text_line_count(font: SituationFont, max_width: f32) -> c_int {
    unsafe { SituationGetTextLineCount(font, max_width) }
}

/// Retrieves the bindless texture handle (OpenGL Only).
pub fn situation_get_texture_handle(texture: SituationTexture) -> u64 {
    unsafe { SituationGetTextureHandle(texture) }
}

/// [Phase 2] Query texture metadata.
pub fn situation_get_texture_info(texture: SituationTexture, out_info: &mut SituationTextureInfo) -> Result<(), SituationError> {
    let err = unsafe { SituationGetTextureInfo(texture, out_info) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Reads affinity mask for the CURRENT thread
pub fn situation_get_thread_affinity(out_mask: &mut u64) -> Result<(), SituationError> {
    let err = unsafe { SituationGetThreadAffinity(out_mask) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// NUMA node for current thread, or -1 if unknown
pub fn situation_get_thread_numa_node() -> c_int {
    unsafe { SituationGetThreadNumaNode() }
}

/// Scheduler counters snapshot
pub fn situation_get_thread_pool_metrics(pool: *mut SituationThreadPool, out_metrics: &mut SituationThreadPoolMetrics) -> Result<(), SituationError> {
    let err = unsafe { SituationGetThreadPoolMetrics(pool, out_metrics) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Worker/I/O/render/audio placement snapshot
pub fn situation_get_thread_pool_snapshot(pool: *mut SituationThreadPool, out: &mut SituationThreadPoolSnapshot) -> Result<(), SituationError> {
    let err = unsafe { SituationGetThreadPoolSnapshot(pool, out) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Runtime threading capabilities + pool summary
pub fn situation_get_threading_status() -> SituationThreadingStatus {
    unsafe { SituationGetThreadingStatus() }
}

/// [Caller frees] Get the full path to the current user's home directory.
pub fn situation_get_user_directory() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetUserDirectory() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Total GPU memory allocated (Bytes)
pub fn situation_get_vram_usage() -> u64 {
    unsafe { SituationGetVRAMUsage() }
}

/// [Main thread] Returns a read-only static string (e.g., "2.4.336"). Do not free.
pub fn situation_get_version_string() -> Option<*const c_char> {
    let ptr = unsafe { SituationGetVersionString() };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Get a pointer to a virtual display's state.
pub fn situation_get_virtual_display(display_id: c_int) -> Option<*mut SituationVirtualDisplay> {
    let ptr = unsafe { SituationGetVirtualDisplay(display_id) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Copy current standby config (no-op if invalid id).
pub fn situation_get_virtual_display_pattern_config(display_id: c_int, out_config: &mut SitVdStandbyConfig) {
    unsafe { SituationGetVirtualDisplayPatternConfig(display_id, out_config) };
}

/// Current standby layer bitmask (0 if invalid id).
pub fn situation_get_virtual_display_pattern_layers(display_id: c_int) -> c_int {
    unsafe { SituationGetVirtualDisplayPatternLayers(display_id) }
}

/// Get the internal resolution of a virtual display.
pub fn situation_get_virtual_display_size(display_id: c_int, width: &mut c_int, height: &mut c_int) {
    unsafe { SituationGetVirtualDisplaySize(display_id, width, height) };
}

/// Get the VD's internal texture as a SituationTexture handle (valid for compute-target VDs).
pub fn situation_get_virtual_display_texture(display_id: c_int, out_texture: &mut SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationGetVirtualDisplayTexture(display_id, out_texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Query last VD content write (not the frame clock).
pub fn situation_get_virtual_display_update_info(display_id: c_int, out_last_content_update_time: &mut f64, out_last_content_update_frame: &mut u64, out_frames_since_update: &mut u64, out_seconds_since_update: &mut f64) -> Result<(), SituationError> {
    let err = unsafe { SituationGetVirtualDisplayUpdateInfo(display_id, out_last_content_update_time, out_last_content_update_frame, out_frames_since_update, out_seconds_since_update) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get the window's top-left position on the screen.
pub fn situation_get_window_position() -> Vector2 {
    unsafe { SituationGetWindowPosition() }
}

/// Get the DPI scaling factor for the window.
pub fn situation_get_window_scale_dpi() -> Vector2 {
    unsafe { SituationGetWindowScaleDPI() }
}

/// Get the current logical window size.
pub fn situation_get_window_size(width: &mut c_int, height: &mut c_int) {
    unsafe { SituationGetWindowSize(width, height) };
}

/// Check if the window is currently focused.
pub fn situation_has_window_focus() -> bool {
    unsafe { SituationHasWindowFocus() }
}

/// Hide the mouse cursor.
pub fn situation_hide_cursor() {
    unsafe { SituationHideCursor() };
}

/// Converts a Hue, Saturation, Value color back to the standard RGBA color space.
pub fn situation_hsv_to_rgb(hsv: ColorHSV) -> ColorRGBA {
    unsafe { SituationHsvToRgb(hsv) }
}

/// Control an image by Hue Saturation and Brightness.
pub fn situation_image_adjust_hsv(hue_shift: f32, sat_factor: f32, val_factor: f32, mix: f32) {
    unsafe { SituationImageAdjustHSV(hue_shift, sat_factor, val_factor, mix) };
}

/// Grade an image in YPQ (phase/chroma/luma).
pub fn situation_image_adjust_ypq(phase_shift_deg: f32, chroma_factor: f32, luma_factor: f32, mix: f32) {
    unsafe { SituationImageAdjustYPQ(phase_shift_deg, chroma_factor, luma_factor, mix) };
}

/// Create a new image by copying another.
pub fn situation_image_copy(image: SituationImage, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationImageCopy(image, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Crop an image to a specific rectangle.
pub fn situation_image_crop(crop: SitRectangle) {
    unsafe { SituationImageCrop(crop) };
}

/// Copying portion of one image into another image at destination placement
pub fn situation_image_draw(src: SituationImage, srcRect: SitRectangle, dstPos: Vector2) {
    unsafe { SituationImageDraw(src, srcRect, dstPos) };
}

/// Draw a portion of a source image onto dst with alpha tinting.
pub fn situation_image_draw_alpha(src: SituationImage, srcRect: SitRectangle, dstPos: Vector2, tint: ColorRGBA) {
    unsafe { SituationImageDrawAlpha(src, srcRect, dstPos, tint) };
}

/// Draw a single Unicode character with advanced styling onto an image.
pub fn situation_image_draw_codepoint(font: SituationFont, codepoint: c_int, position: Vector2, fontSize: f32, rotationDegrees: f32, skewFactor: f32, fillColor: ColorRGBA, outlineColor: ColorRGBA, outlineThickness: f32) {
    unsafe { SituationImageDrawCodepoint(font, codepoint, position, fontSize, rotationDegrees, skewFactor, fillColor, outlineColor, outlineThickness) };
}

/// Draw a simple, tinted text string onto an image.
pub fn situation_image_draw_text(font: SituationFont, position: Vector2, fontSize: f32, spacing: f32, tint: ColorRGBA) {
    unsafe { SituationImageDrawText(font, position, fontSize, spacing, tint) };
}

/// Draw a text string with advanced styling (rotation, outline) onto an image.
pub fn situation_image_draw_text_ex(font: SituationFont, position: Vector2, fontSize: f32, spacing: f32, rotationDegrees: f32, skewFactor: f32, fillColor: ColorRGBA, outlineColor: ColorRGBA, outlineThickness: f32) {
    unsafe { SituationImageDrawTextEx(font, position, fontSize, spacing, rotationDegrees, skewFactor, fillColor, outlineColor, outlineThickness) };
}

/// Flip an image.
pub fn situation_image_flip(mode: SituationImageFlipMode) {
    unsafe { SituationImageFlip(mode) };
}

/// Resize an image using default bicubic scaling.
pub fn situation_image_resize(newWidth: c_int, newHeight: c_int) {
    unsafe { SituationImageResize(newWidth, newHeight) };
}

pub fn situation_image_stamp_text(dst: &mut SituationImage, font: SituationFont, text: &CStr, pos: Vector2, fontSize: f32, text_color: ColorRGBA, bg_color: ColorRGBA) -> Result<(), SituationError> {
    let err = unsafe { SituationImageStampText(dst, font, text.as_ptr(), pos, fontSize, text_color, bg_color) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

pub fn situation_image_stamp_text_boxed(dst: &mut SituationImage, font: SituationFont, text: &CStr, bounds: SitRectangle, fontSize: f32, text_color: ColorRGBA, bg_color: ColorRGBA, word_wrap: bool, out_width: &mut c_int, out_height: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationImageStampTextBoxed(dst, font, text.as_ptr(), bounds, fontSize, text_color, bg_color, word_wrap, out_width, out_height) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Main thread] Initialize the library, create window and graphics context.
pub fn situation_init(argc: c_int, argv: *const *const c_char, init_info: &mut SituationInitInfo) -> Result<(), SituationError> {
    let err = unsafe { SituationInit(argc, argv, init_info) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Initialize the built-in device registry (call once at startup).
pub fn situation_init_device_registry() {
    unsafe { SituationInitDeviceRegistry() };
}

/// Check if the application is currently paused.
pub fn situation_is_app_paused() -> bool {
    unsafe { SituationIsAppPaused() }
}

/// Check if a command-line argument (e.g., "-server") was provided.
pub fn situation_is_argument_present(arg_name: &CStr) -> bool {
    unsafe { SituationIsArgumentPresent(arg_name.as_ptr()) }
}

/// Check if the audio device is currently playing.
pub fn situation_is_audio_device_playing() -> bool {
    unsafe { SituationIsAudioDevicePlaying() }
}

/// Check if a device type is registered.
pub fn situation_is_device_registered(r#type: SituationNodeType) -> bool {
    unsafe { SituationIsDeviceRegistered(r#type) }
}

/// Check if a graphics feature is supported on current hardware.
pub fn situation_is_feature_supported(feature: SituationRenderFeature) -> bool {
    unsafe { SituationIsFeatureSupported(feature) }
}

/// Check if a file was dropped into the window this frame.
pub fn situation_is_file_dropped() -> bool {
    unsafe { SituationIsFileDropped() }
}

/// Check if a connected joystick has a standard gamepad mapping.
pub fn situation_is_gamepad(jid: c_int) -> bool {
    unsafe { SituationIsGamepad(jid) }
}

/// Check if a gamepad button is currently held down (a state).
pub fn situation_is_gamepad_button_down(jid: c_int, button: c_int) -> bool {
    unsafe { SituationIsGamepadButtonDown(jid, button) }
}

/// Check if a gamepad button was pressed down this frame (an event).
pub fn situation_is_gamepad_button_pressed(jid: c_int, button: c_int) -> bool {
    unsafe { SituationIsGamepadButtonPressed(jid, button) }
}

/// Check if a gamepad button was released this frame (an event).
pub fn situation_is_gamepad_button_released(jid: c_int, button: c_int) -> bool {
    unsafe { SituationIsGamepadButtonReleased(jid, button) }
}

/// Check if an image has been loaded successfully.
pub fn situation_is_image_valid(image: SituationImage) -> bool {
    unsafe { SituationIsImageValid(image) }
}

/// [Main thread] Check if the library has been successfully initialized.
pub fn situation_is_initialized() -> bool {
    unsafe { SituationIsInitialized() }
}

/// Check if a joystick/gamepad is connected.
pub fn situation_is_joystick_present(jid: c_int) -> bool {
    unsafe { SituationIsJoystickPresent(jid) }
}

/// Check if a key is currently held down (a state).
pub fn situation_is_key_down(key: c_int) -> bool {
    unsafe { SituationIsKeyDown(key) }
}

/// Check if a key was pressed down this frame (an event).
pub fn situation_is_key_pressed(key: c_int) -> bool {
    unsafe { SituationIsKeyPressed(key) }
}

/// Check if a key was released this frame (an event).
pub fn situation_is_key_released(key: c_int) -> bool {
    unsafe { SituationIsKeyReleased(key) }
}

/// Check if a key is currently up (a state).
pub fn situation_is_key_up(key: c_int) -> bool {
    unsafe { SituationIsKeyUp(key) }
}

/// Check if currently in learn mode. Returns 1/0.
pub fn situation_is_learning(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> c_int {
    unsafe { SituationIsLearning(graph, handle) }
}

/// Check if a lock key (Caps, Num) is currently active.
pub fn situation_is_lock_key_pressed(lock_key_mod: c_int) -> bool {
    unsafe { SituationIsLockKeyPressed(lock_key_mod) }
}

/// Check if a node has MIDI control enabled. Returns 1/0.
pub fn situation_is_midi_enabled(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> c_int {
    unsafe { SituationIsMidiEnabled(graph, handle) }
}

/// Check if MIDI Learn is enabled. Returns 1/0.
pub fn situation_is_midi_learn_enabled(graph: *mut SituationAudioGraph, handle: SituationNodeHandle) -> c_int {
    unsafe { SituationIsMidiLearnEnabled(graph, handle) }
}

/// Check if a modifier key (Shift, Ctrl, Alt) is pressed.
pub fn situation_is_modifier_pressed(modifier: c_int) -> bool {
    unsafe { SituationIsModifierPressed(modifier) }
}

/// Check if a mouse button is currently held down (a state).
pub fn situation_is_mouse_button_down(button: c_int) -> bool {
    unsafe { SituationIsMouseButtonDown(button) }
}

/// Check if a mouse button was pressed down this frame (an event).
pub fn situation_is_mouse_button_pressed(button: c_int) -> bool {
    unsafe { SituationIsMouseButtonPressed(button) }
}

/// Check if a mouse button was released this frame.
pub fn situation_is_mouse_button_released(button: c_int) -> bool {
    unsafe { SituationIsMouseButtonReleased(button) }
}

/// Check if a physical key (scancode) is currently held down.
pub fn situation_is_scancode_down(scancode: c_int) -> bool {
    unsafe { SituationIsScancodeDown(scancode) }
}

/// Check if Scroll Lock is currently toggled on.
pub fn situation_is_scroll_lock_on() -> bool {
    unsafe { SituationIsScrollLockOn() }
}

/// True for stb_image decode extensions (.jpg, .png, .bmp, .tga, .psd, .gif, .hdr, .pic, .ppm, .pgm, .pnm).
pub fn situation_is_stb_image_load_extension(extension: &CStr) -> bool {
    unsafe { SituationIsStbImageLoadExtension(extension.as_ptr()) }
}

/// Check if a serialized version is compatible with this library.
pub fn situation_is_version_compatible(json_version: &CStr) -> bool {
    unsafe { SituationIsVersionCompatible(json_version.as_ptr()) }
}

/// Check if a virtual display is marked as dirty.
pub fn situation_is_virtual_display_dirty(display_id: c_int) -> bool {
    unsafe { SituationIsVirtualDisplayDirty(display_id) }
}

/// Check if the window is currently in fullscreen mode.
pub fn situation_is_window_fullscreen() -> bool {
    unsafe { SituationIsWindowFullscreen() }
}

/// Check if the window is currently hidden.
pub fn situation_is_window_hidden() -> bool {
    unsafe { SituationIsWindowHidden() }
}

/// Check if the window is currently maximized.
pub fn situation_is_window_maximized() -> bool {
    unsafe { SituationIsWindowMaximized() }
}

/// Check if the window is currently minimized.
pub fn situation_is_window_minimized() -> bool {
    unsafe { SituationIsWindowMinimized() }
}

/// Check if the window was resized in the last frame.
pub fn situation_is_window_resized() -> bool {
    unsafe { SituationIsWindowResized() }
}

/// Check if a specific window state flag is set.
pub fn situation_is_window_state(flag: u32) -> bool {
    unsafe { SituationIsWindowState(flag) }
}

/// Join two path components with the correct OS separator (caller must free).
pub fn situation_join_path(base_path: &CStr, file_or_dir_name: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationJoinPath(base_path.as_ptr(), file_or_dir_name.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Linear display light [0,1] → ST.2084 PQ [0,1].
pub fn situation_linear_to_pq(linear: f32) -> f32 {
    unsafe { SituationLinearToPq(linear) }
}

/// List files and subdirectories in a path (caller must free with SituationFreeDirectoryFileList).
pub fn situation_list_directory_files(dir_path: &CStr, out_count: &mut c_int) -> Option<*const *const c_char> {
    let ptr = unsafe { SituationListDirectoryFiles(dir_path.as_ptr(), out_count) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// List available MIDI input devices. Returns number found.
pub fn situation_list_midi_devices(devices: &mut SituationMidiDeviceInfo, max_count: c_int) -> c_int {
    unsafe { SituationListMidiDevices(devices, max_count) }
}

/// Load audio and return a lightweight handle for playback control.
pub fn situation_load_audio(file_path: &CStr, mode: SituationAudioLoadMode, looping: bool) -> SituationSoundHandle {
    unsafe { SituationLoadAudio(file_path.as_ptr(), mode, looping) }
}

/// Loads a raw bitmap font (e.g. 8x8 array).
pub fn situation_load_bitmap_font_from_memory(data: &mut u8, char_width: c_int, char_height: c_int, num_chars: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadBitmapFontFromMemory(data, char_width, char_height, num_chars, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Grid atlas already on GPU — fills layout metadata.
pub fn situation_load_bitmap_font_from_texture(sheet: SituationTexture, char_width: c_int, char_height: c_int, first_char: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadBitmapFontFromTexture(sheet, char_width, char_height, first_char, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get the paths of dropped files (returns a copy, caller must free).
pub fn situation_load_dropped_files(count: &mut c_int) -> Option<*const *const c_char> {
    let ptr = unsafe { SituationLoadDroppedFiles(count) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Asynchronously load a file.
pub fn situation_load_file_async(pool: *mut SituationThreadPool, file_path: &CStr, callback: SituationFileLoadCallback, user_data: *mut c_void) -> SituationJobId {
    unsafe { SituationLoadFileAsync(pool, file_path.as_ptr(), callback, user_data) }
}

/// Load an entire file into a memory buffer (caller must free).
pub fn situation_load_file_data(file_path: &CStr, out_bytes_read: &mut c_uint, out_data: &mut u8) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadFileData(file_path.as_ptr(), out_bytes_read, out_data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load a text file into a null-terminated string (caller must free).
pub fn situation_load_file_text(file_path: &CStr) -> Option<*const c_char> {
    let ptr = unsafe { SituationLoadFileText(file_path.as_ptr()) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Asynchronously load a text file.
pub fn situation_load_file_text_async(pool: *mut SituationThreadPool, file_path: &CStr, callback: SituationFileTextLoadCallback, user_data: *mut c_void) -> SituationJobId {
    unsafe { SituationLoadFileTextAsync(pool, file_path.as_ptr(), callback, user_data) }
}

/// Load a font from a TTF/OTF file for CPU rendering.
pub fn situation_load_font(out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadFont(out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Loads a font directly from a memory buffer (e.g., embedded resource).
pub fn situation_load_font_from_memory(data: *mut c_void, dataSize: c_int, out_font: &mut SituationFont) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadFontFromMemory(data, dataSize, out_font) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load a graph from a JSON file, re-creating nodes via device_funcs.
pub fn situation_load_graph_from_file(graph: *mut SituationAudioGraph, filepath: &CStr, device_funcs: &mut SituationDeviceFunctions, num_device_funcs: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadGraphFromFile(graph, filepath.as_ptr(), device_funcs, num_device_funcs) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load an image via stb_image (JPEG, PNG, BMP, TGA, PSD, GIF, HDR, PIC, PNM).
pub fn situation_load_image(out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadImage(out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load an image from a memory buffer.
pub fn situation_load_image_from_memory(dataSize: c_int, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadImageFromMemory(dataSize, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Get a copy of the current screen backbuffer as an image.
pub fn situation_load_image_from_screen(out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadImageFromScreen(out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load MIDI Learn mappings from JSON file.
pub fn situation_load_midi_preset(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, filename: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadMidiPreset(graph, handle, filename.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Loads a complete 3D model and its textures from a GLTF file.
pub fn situation_load_model(file_path: &CStr, out_model: &mut SituationModel) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadModel(file_path.as_ptr(), out_model) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Wavefront OBJ: triangulated meshes, MTL/textures; missing/degenerate normals filled from face geometry, authored normals preserved.
pub fn situation_load_model_from_obj(file_path: &CStr, out_model: &mut SituationModel) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadModelFromOBJ(file_path.as_ptr(), out_model) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load a graphics shader pipeline from vertex and fragment files.
pub fn situation_load_shader(vs_path: &CStr, fs_path: &CStr, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadShader(vs_path.as_ptr(), fs_path.as_ptr(), out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Create a graphics shader pipeline from in-memory GLSL source.
pub fn situation_load_shader_from_memory(vs_code: &CStr, fs_code: &CStr, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadShaderFromMemory(vs_code.as_ptr(), fs_code.as_ptr(), out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Precompiled .spv: OpenGL via GL_ARB_gl_spirv; Vulkan same pipeline contract as SituationLoadShaderFromMemory (no shaderc required).
pub fn situation_load_shader_from_spirv(vs_spv_path: &CStr, fs_spv_path: &CStr, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadShaderFromSpirv(vs_spv_path.as_ptr(), fs_spv_path.as_ptr(), out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Same as SituationLoadShaderFromSpirv but from in-memory SPIR-V (e.g. build-time embedded .spv). No hot-reload paths (vs/fs file paths left unset).
pub fn situation_load_shader_from_spirv_memory(vs_spirv: *mut c_void, vs_len: usize, fs_spirv: *mut c_void, fs_len: usize, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadShaderFromSpirvMemory(vs_spirv, vs_len, fs_spirv, fs_len, out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Vulkan] `layout_profile` selects user SSBO/UBO layouts; [OpenGL] profile ignored.
pub fn situation_load_shader_from_spirv_memory_ex(vs_spirv: *mut c_void, vs_len: usize, fs_spirv: *mut c_void, fs_len: usize, layout_profile: SituationSpirvLayoutProfile, out_shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadShaderFromSpirvMemoryEx(vs_spirv, vs_len, fs_spirv, fs_len, layout_profile, out_shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Load a sound from a file.
pub fn situation_load_sound_from_file(file_path: &CStr, mode: SituationAudioLoadMode, looping: bool, out_sound: &mut SituationSound) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadSoundFromFile(file_path.as_ptr(), mode, looping, out_sound) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Asynchronously loads and decodes a sound file.
pub fn situation_load_sound_from_file_async(pool: *mut SituationThreadPool, file_path: &CStr, looping: bool, out_sound: &mut SituationSound) -> SituationJobId {
    unsafe { SituationLoadSoundFromFileAsync(pool, file_path.as_ptr(), looping, out_sound) }
}

/// Load a sound from a custom stream.
pub fn situation_load_sound_from_stream(on_read: SituationStreamReadCallback, on_seek: SituationStreamSeekCallback, user_data: *mut c_void, format: &mut SituationAudioFormat, looping: bool, out_sound: &mut SituationSound) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadSoundFromStream(on_read, on_seek, user_data, format, looping, out_sound) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Loads a texture from disk and registers the path for hot-reloading.
pub fn situation_load_texture(file_path: &CStr, generate_mipmaps: bool, out_texture: &mut SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationLoadTexture(file_path.as_ptr(), generate_mipmaps, out_texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Maximize the window if it's resizable.
pub fn situation_maximize_window() {
    unsafe { SituationMaximizeWindow() };
}

/// Measure the pixel dimensions of a string before drawing.
pub fn situation_measure_text(font: SituationFont, fontSize: f32) -> SitRectangle {
    unsafe { SituationMeasureText(font, fontSize) }
}

/// Measure with extra per-character spacing.
pub fn situation_measure_text_ex(font: SituationFont, fontSize: f32, spacing: f32) -> SitRectangle {
    unsafe { SituationMeasureTextEx(font, fontSize, spacing) }
}

pub fn situation_memory_barrier(cmd: SituationCommandBuffer, barrier_bits: u32) {
    unsafe { SituationMemoryBarrier(cmd, barrier_bits) };
}

/// Minimize the window (iconify).
pub fn situation_minimize_window() {
    unsafe { SituationMinimizeWindow() };
}

/// Move/rename a file, even across drives on Windows.
pub fn situation_move_file(old_path: &CStr, new_path: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationMoveFile(old_path.as_ptr(), new_path.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Open a file or folder with its default application.
pub fn situation_open_file(filePath: &CStr) {
    unsafe { SituationOpenFile(filePath.as_ptr()) };
}

/// Pause the application's internal state (e.g., audio).
pub fn situation_pause_app() {
    unsafe { SituationPauseApp() };
}

/// Pause audio playback on the device.
pub fn situation_pause_audio_device() -> Result<(), SituationError> {
    let err = unsafe { SituationPauseAudioDevice() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Peek at the next key in the press queue without consuming it.
pub fn situation_peek_key_pressed() -> c_int {
    unsafe { SituationPeekKeyPressed() }
}

/// Peek at the next key and its scancode.
pub fn situation_peek_key_pressed_ex(out_scancode: &mut c_int) -> c_int {
    unsafe { SituationPeekKeyPressedEx(out_scancode) }
}

/// Play audio by handle (restarts if already playing).
pub fn situation_play_audio(handle: SituationSoundHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationPlayAudio(handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Play a loaded sound (restarts if already playing).
pub fn situation_play_loaded_sound(sound: &mut SituationSound) -> Result<(), SituationError> {
    let err = unsafe { SituationPlayLoadedSound(sound) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Legacy: play a tone by MIDI note number (0-127).
pub fn situation_play_midi_note(note: c_int, r#type: SituationWaveType, volume: f32, attack_sec: f32, decay_sec: f32, sustain_level: f32, release_sec: f32, hold_sec: f32) {
    unsafe { SituationPlayMidiNote(note, r#type, volume, attack_sec, decay_sec, sustain_level, release_sec, hold_sec) };
}

/// Legacy: play a simple ADSR tone (backward compat / quick UI sounds).
pub fn situation_play_tone(r#type: SituationWaveType, frequency: f32, volume: f32, attack_sec: f32, decay_sec: f32, sustain_level: f32, release_sec: f32, hold_sec: f32) {
    unsafe { SituationPlayTone(r#type, frequency, volume, attack_sec, decay_sec, sustain_level, release_sec, hold_sec) };
}

/// @brief Plays an extended procedural tone with full control. @param type          Waveform type (Sine, Square, Triangle, Saw, Noise) @param frequency     Frequency in Hz (e.g., 440.0f). For noise: ignored (use 0.0f) @param volume        Peak volume (0.0 to 1.0) @param pan           Stereo panning (-1.0 left, 0.0 center, +1.0 right) @param attack_sec    Attack time in seconds @param decay_sec     Decay time in seconds @param sustain_level Sustain volume level (0.0 to 1.0) @param release_sec   Release time in seconds @param hold_sec      Hold duration in seconds. Use -1.0f for infinite sustain (key down) @return Handle to the playing tone, or 0 if no voice available (polyphony limit)
pub fn situation_play_tone_ex(r#type: SituationWaveType, frequency: f32, volume: f32, pan: f32, attack_sec: f32, decay_sec: f32, sustain_level: f32, release_sec: f32, hold_sec: f32) -> SituationToneHandle {
    unsafe { SituationPlayToneEx(r#type, frequency, volume, pan, attack_sec, decay_sec, sustain_level, release_sec, hold_sec) }
}

/// [Main thread] Poll for all input events (keyboard, mouse, joystick). Call once per frame.
pub fn situation_poll_input_events() {
    unsafe { SituationPollInputEvents() };
}

/// SITUATION_SUCCESS when ready, SITUATION_ERROR_SHADER_LOAD_IN_PROGRESS while compiling/linking/building pipelines.
pub fn situation_poll_shader_load(shader: SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationPollShaderLoad(shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Uniform PQ gray → A2R10G10B10 packed pixel.
pub fn situation_pq_gray_to_rgb10_packed(pq_level: f32) -> u32 {
    unsafe { SituationPqGrayToRgb10Packed(pq_level) }
}

/// ST.2084 PQ [0,1] → linear display light [0,1].
pub fn situation_pq_to_linear(pq: f32) -> f32 {
    unsafe { SituationPqToLinear(pq) }
}

/// Human-readable threading status (stdout if NULL)
pub fn situation_print_threading_status(out_stream: &mut FILE) {
    unsafe { SituationPrintThreadingStatus(out_stream) };
}

/// Push interleaved float PCM into a PCM_INPUT node's ring buffer (any thread). Returns frames written.
pub fn situation_push_node_pcm(graph: *mut SituationAudioGraph, node: SituationNodeHandle, samples: &mut f32, frame_count: u32, channels: u32) -> u32 {
    unsafe { SituationPushNodePCM(graph, node, samples, frame_count, channels) }
}

/// [OpenGL] Enumerate all active SSBO blocks and their assigned binding points. out_count receives actual block count (may exceed capacity). Returns NOT_IMPLEMENTED on Vulkan.
pub fn situation_query_shader_storage_blocks(shader: SituationShader, out_blocks: &mut SituationShaderStorageBlockInfo, capacity: c_int, out_count: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationQueryShaderStorageBlocks(shader, out_blocks, capacity, out_count) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Read mapped buffer data safely.
pub fn situation_read_buffer(readback_buf: SituationBuffer, dst: *mut c_void, size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationReadBuffer(readback_buf, dst, size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Phase 2] Blocking readback of framebuffer pixels (RGBA8 or raw RGB10 packed).
pub fn situation_read_framebuffer(desc: &mut SituationReadPixelsDesc, dst_pixels: *mut c_void, dst_size_bytes: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationReadFramebuffer(desc, dst_pixels, dst_size_bytes) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Phase 8] Raw A2R10G10B10 readback when HDR10 swapchain active.
pub fn situation_read_framebuffer_hdr(desc: &mut SituationReadPixelsDesc, dst_pixels: &mut u32, dst_size_bytes: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationReadFramebufferHdr(desc, dst_pixels, dst_size_bytes) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Phase 2] Blocking readback of texture pixels.
pub fn situation_read_texture(texture: SituationTexture, desc: &mut SituationTextureReadbackDesc, dst_pixels: *mut c_void, dst_size_bytes: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationReadTexture(texture, desc, dst_pixels, dst_size_bytes) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Phase 2] Blocking readback into allocated SituationImage.
pub fn situation_read_texture_alloc(texture: SituationTexture, desc: &mut SituationTextureReadbackDesc, out_image: &mut SituationImage) -> Result<(), SituationError> {
    let err = unsafe { SituationReadTextureAlloc(texture, desc, out_image) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Rebuilds the process-wide topology cache
pub fn situation_refresh_cpu_topology() -> Result<(), SituationError> {
    let err = unsafe { SituationRefreshCpuTopology() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Force a refresh of the cached display information.
pub fn situation_refresh_displays() {
    unsafe { SituationRefreshDisplays() };
}

/// Rebuild NUMA summary from CPU topology + OS memory
pub fn situation_refresh_numa_topology() -> Result<(), SituationError> {
    let err = unsafe { SituationRefreshNumaTopology() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Register a custom device type with the registry.
pub fn situation_register_device_type(meta: &mut SituationDeviceMetadata) -> Result<(), SituationError> {
    let err = unsafe { SituationRegisterDeviceType(meta) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Recompiles a compute pipeline from its original source file (Synchronous/Stalls GPU).
pub fn situation_reload_compute_pipeline(pipeline: &mut SituationComputePipeline) -> Result<(), SituationError> {
    let err = unsafe { SituationReloadComputePipeline(pipeline) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Re-parses GLTF/GLB file and rebuilds all meshes and textures (Synchronous/Stalls GPU).
pub fn situation_reload_model(model: &mut SituationModel) -> Result<(), SituationError> {
    let err = unsafe { SituationReloadModel(model) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Recompiles and links a shader from its original source files (Synchronous/Stalls GPU).
pub fn situation_reload_shader(shader: &mut SituationShader) -> Result<(), SituationError> {
    let err = unsafe { SituationReloadShader(shader) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Re-reads image file and recreates the GPU texture resource (Synchronous/Stalls GPU).
pub fn situation_reload_texture(texture: &mut SituationTexture) -> Result<(), SituationError> {
    let err = unsafe { SituationReloadTexture(texture) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Disconnect a specific patch between two ports.
pub fn situation_remove_patch(graph: *mut SituationAudioGraph, src: SituationNodeHandle, src_port: c_int, dst: SituationNodeHandle, dst_port: c_int, is_control: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationRemovePatch(graph, src, src_port, dst, dst_port, is_control) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Alias for SituationMoveFile.
pub fn situation_rename_file(old_path: &CStr, new_path: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationRenameFile(old_path.as_ptr(), new_path.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Fill pass struct from VD attachment defaults (tier C helper).
pub fn situation_render_pass_info_inherit(display_id: c_int) -> SituationRenderPassInfo {
    unsafe { SituationRenderPassInfoInherit(display_id) }
}

/// Composite all visible virtual displays to the current target.
pub fn situation_render_virtual_displays(cmd: SituationCommandBuffer) -> Result<(), SituationError> {
    let err = unsafe { SituationRenderVirtualDisplays(cmd) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Replay a previously recorded render list into a command buffer.
pub fn situation_replay_render_list(cmd: SituationCommandBuffer, list: SituationRenderList) {
    unsafe { SituationReplayRenderList(cmd, list) };
}

pub fn situation_request_screen_capture() {
    unsafe { SituationRequestScreenCapture() };
}

/// Reset a render list for reuse next frame.
pub fn situation_reset_render_list(list: SituationRenderList) {
    unsafe { SituationResetRenderList(list) };
}

/// Zero scheduler counters
pub fn situation_reset_thread_pool_stats(pool: *mut SituationThreadPool) {
    unsafe { SituationResetThreadPoolStats(pool) };
}

/// Restore a minimized or maximized window.
pub fn situation_restore_window() {
    unsafe { SituationRestoreWindow() };
}

/// Resume a paused application.
pub fn situation_resume_app() {
    unsafe { SituationResumeApp() };
}

/// Resume audio playback on the device.
pub fn situation_resume_audio_device() -> Result<(), SituationError> {
    let err = unsafe { SituationResumeAudioDevice() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Upscale 8-bit RGBA → 10-bit.
pub fn situation_rgb10_from_rgba(color: ColorRGBA) -> ColorRGBA10 {
    unsafe { SituationRgb10FromRgba(color) }
}

/// Converts a standard RGBA color to the Hue, Saturation, Value color space.
pub fn situation_rgb_to_hsv(rgb: ColorRGBA) -> ColorHSV {
    unsafe { SituationRgbToHsv(rgb) }
}

/// 10-bit RGBA → float YPQ.
pub fn situation_rgb_to_ypq_from10(color: ColorRGBA10) -> ColorYPQf {
    unsafe { SituationRgbToYpqFrom10(color) }
}

/// Downscale 10-bit RGBA → 8-bit.
pub fn situation_rgba_from_rgb10(color: ColorRGBA10) -> ColorRGBA {
    unsafe { SituationRgbaFromRgb10(color) }
}

/// A2R10G10B10 texel → RGBA8 (readback parity).
pub fn situation_rgba_from_rgb10_packed(packed: u32) -> ColorRGBA {
    unsafe { SituationRgbaFromRgb10Packed(packed) }
}

/// Asynchronously save a file.
pub fn situation_save_file_async(pool: *mut SituationThreadPool, file_path: &CStr, data: *mut c_void, size: usize, callback: SituationFileSaveCallback, user_data: *mut c_void) -> SituationJobId {
    unsafe { SituationSaveFileAsync(pool, file_path.as_ptr(), data, size, callback, user_data) }
}

/// Save a block of memory to a file.
pub fn situation_save_file_data(file_path: &CStr, data: *mut c_void, bytes_to_write: c_uint) -> Result<(), SituationError> {
    let err = unsafe { SituationSaveFileData(file_path.as_ptr(), data, bytes_to_write) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Save a null-terminated string to a text file.
pub fn situation_save_file_text(file_path: &CStr, text: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSaveFileText(file_path.as_ptr(), text.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Asynchronously save a text file.
pub fn situation_save_file_text_async(pool: *mut SituationThreadPool, file_path: &CStr, text: &CStr, callback: SituationFileSaveCallback, user_data: *mut c_void) -> SituationJobId {
    unsafe { SituationSaveFileTextAsync(pool, file_path.as_ptr(), text.as_ptr(), callback, user_data) }
}

/// Save a graph to a JSON file.
pub fn situation_save_graph_to_file(graph: *mut SituationAudioGraph, filepath: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSaveGraphToFile(graph, filepath.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Save MIDI Learn mappings to JSON file.
pub fn situation_save_midi_preset(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, filename: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSaveMidiPreset(graph, handle, filename.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Exports a model to a human-readable .gltf and a .bin file for debugging.
pub fn situation_save_model_as_gltf(model: SituationModel, file_path: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSaveModelAsGltf(model, file_path.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Serialize a graph to a JSON string (caller must free with SituationFreeJSONString).
pub fn situation_serialize_graph_to_json(graph: *mut SituationAudioGraph) -> Option<*const c_char> {
    let ptr = unsafe { SituationSerializeGraphToJSON(graph) };
    if ptr.is_null() {
        None
    } else {
        Some(ptr)
    }
}

/// Set the active audio processing graph (replaces default). NULL disables graph processing.
pub fn situation_set_active_graph(graph: *mut SituationAudioGraph) -> Result<(), SituationError> {
    let err = unsafe { SituationSetActiveGraph(graph) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the active audio device.
pub fn situation_set_audio_device(internal_id: c_int, format: &mut SituationAudioFormat) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioDevice(internal_id, format) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the master volume for the audio device.
pub fn situation_set_audio_master_volume(volume: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioMasterVolume(volume) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set a callback to receive mixed output samples (for VU meters, FFT, etc.).
pub fn situation_set_audio_output_monitor(user_data: *mut c_void) {
    unsafe { SituationSetAudioOutputMonitor(user_data) };
}

/// Set stereo pan for a handle-based sound [-1.0 to 1.0].
pub fn situation_set_audio_pan(handle: SituationSoundHandle, pan: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioPan(handle, pan) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set pitch multiplier for a handle-based sound (1.0 = normal).
pub fn situation_set_audio_pitch(handle: SituationSoundHandle, pitch: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioPitch(handle, pitch) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Re-initialize the audio device with a new sample rate.
pub fn situation_set_audio_playback_sample_rate(sample_rate: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioPlaybackSampleRate(sample_rate) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set volume for a handle-based sound [0.0 to 1.0+].
pub fn situation_set_audio_volume(handle: SituationSoundHandle, volume: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetAudioVolume(handle, volume) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set text in the system clipboard.
pub fn situation_set_clipboard_text(text: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSetClipboardText(text.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set a control parameter on a node.
pub fn situation_set_control(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, control_id: u32, value: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetControl(graph, handle, control_id, value) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// OS-visible name for the calling thread (UTF-8); no-op if NULL/empty
pub fn situation_set_current_thread_name(name: &CStr) {
    unsafe { SituationSetCurrentThreadName(name.as_ptr()) };
}

/// Set the mouse cursor to a standard shape.
pub fn situation_set_cursor(cursor: SituationCursor) {
    unsafe { SituationSetCursor(cursor) };
}

/// Set a callback for mouse movement events.
pub fn situation_set_cursor_pos_callback(callback: SituationCursorPosCallback, user_data: *mut c_void) {
    unsafe { SituationSetCursorPosCallback(callback, user_data) };
}

/// Set the display mode for a monitor.
pub fn situation_set_display_mode(monitor_id: c_int, mode: &mut SituationDisplayMode, fullscreen: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationSetDisplayMode(monitor_id, mode, fullscreen) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set a callback to run just before shutdown.
pub fn situation_set_exit_callback(user_data: *mut c_void) {
    unsafe { SituationSetExitCallback(user_data) };
}

/// Set a callback for file drop events.
pub fn situation_set_file_drop_callback(callback: SituationFileDropCallback, user_data: *mut c_void) {
    unsafe { SituationSetFileDropCallback(callback, user_data) };
}

/// Set a callback for window focus events.
pub fn situation_set_focus_callback(callback: SituationFocusCallback, user_data: *mut c_void) {
    unsafe { SituationSetFocusCallback(callback, user_data) };
}

/// Load a new set of gamepad mappings from a string.
pub fn situation_set_gamepad_mappings() -> c_int {
    unsafe { SituationSetGamepadMappings() }
}

/// Set gamepad vibration/rumble (Windows only).
pub fn situation_set_gamepad_vibration(jid: c_int, left_motor: f32, right_motor: f32) -> bool {
    unsafe { SituationSetGamepadVibration(jid, left_motor, right_motor) }
}

/// Designate the Sound Source node in the active graph to receive routed SFX tones.
pub fn situation_set_graph_sfx_source(handle: SituationNodeHandle) -> Result<(), SituationError> {
    let err = unsafe { SituationSetGraphSFXSource(handle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set a callback for joystick connection events.
pub fn situation_set_joystick_callback(callback: SituationJoystickCallback, user_data: *mut c_void) {
    unsafe { SituationSetJoystickCallback(callback, user_data) };
}

/// Set a callback for key events.
pub fn situation_set_key_callback(callback: SituationKeyCallback, user_data: *mut c_void) {
    unsafe { SituationSetKeyCallback(callback, user_data) };
}

/// Set a callback for window maximize / restore events.
pub fn situation_set_maximize_callback(callback: SituationMaximizeCallback, user_data: *mut c_void) {
    unsafe { SituationSetMaximizeCallback(callback, user_data) };
}

/// Set a callback for mouse button events.
pub fn situation_set_mouse_button_callback(callback: SituationMouseButtonCallback, user_data: *mut c_void) {
    unsafe { SituationSetMouseButtonCallback(callback, user_data) };
}

/// Set a software offset for the mouse position.
pub fn situation_set_mouse_offset(offset: Vector2) {
    unsafe { SituationSetMouseOffset(offset) };
}

/// Set the mouse position within the window.
pub fn situation_set_mouse_position(pos: Vector2) {
    unsafe { SituationSetMousePosition(pos) };
}

/// Set a software scale for the mouse position and delta.
pub fn situation_set_mouse_scale(scale: Vector2) {
    unsafe { SituationSetMouseScale(scale) };
}

/// Filter MIDI to channel 0-15, or -1 omni.
pub fn situation_set_node_midi_channel(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, channel: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationSetNodeMidiChannel(graph, handle, channel) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Helper to set a specific pixel color (CPU-side).
pub fn situation_set_pixel_color(x: c_int, y: c_int, col: ColorRGBA) {
    unsafe { SituationSetPixelColor(x, y, col) };
}

/// Set a callback for window framebuffer resize events.
pub fn situation_set_resize_callback(user_data: *mut c_void) {
    unsafe { SituationSetResizeCallback(user_data) };
}

/// Set the default screenshot file format (default: BMP).
pub fn situation_set_screenshot_format(format: SituationScreenshotFormat) {
    unsafe { SituationSetScreenshotFormat(format) };
}

/// Set a callback for mouse scroll events.
pub fn situation_set_scroll_callback(callback: SituationScrollCallback, user_data: *mut c_void) {
    unsafe { SituationSetScrollCallback(callback, user_data) };
}

/// [OpenGL] Set a standalone uniform by name (location cache). While a frame is active, defers to SIT_OP_SET_UNIFORM.
pub fn situation_set_shader_uniform(shader: SituationShader, uniform_name: &CStr, data: *mut c_void, r#type: SituationUniformType) -> Result<(), SituationError> {
    let err = unsafe { SituationSetShaderUniform(shader, uniform_name.as_ptr(), data, r#type) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] Set float uniform array.
pub fn situation_set_shader_uniform1fv(shader: SituationShader, uniform_name: &CStr, count: c_int, values: &mut f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetShaderUniform1fv(shader, uniform_name.as_ptr(), count, values) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] Set int uniform array in one call (e.g. name "uWallRows[0]", count=24). While a frame is active, records SIT_OP_SET_UNIFORM (same as render-thread mode).
pub fn situation_set_shader_uniform1iv(shader: SituationShader, uniform_name: &CStr, count: c_int, values: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationSetShaderUniform1iv(shader, uniform_name.as_ptr(), count, values) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] Set uniform by explicit location (SPIR-V layout(location=)); defers during frames like SituationSetShaderUniform.
pub fn situation_set_shader_uniform_location(shader: SituationShader, location: c_int, data: *mut c_void, r#type: SituationUniformType) -> Result<(), SituationError> {
    let err = unsafe { SituationSetShaderUniformLocation(shader, location, data, r#type) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [OpenGL] Set mat4 uniform array.
pub fn situation_set_shader_uniform_matrix4fv(shader: SituationShader, uniform_name: &CStr, count: c_int, matrices: &mut Mat4) -> Result<(), SituationError> {
    let err = unsafe { SituationSetShaderUniformMatrix4fv(shader, uniform_name.as_ptr(), count, matrices) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Apply an echo effect to a sound.
pub fn situation_set_sound_echo(sound: &mut SituationSound, enabled: bool, delay_sec: f32, feedback: f32, wet_mix: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundEcho(sound, enabled, delay_sec, feedback, wet_mix) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Apply a low-pass or high-pass filter to a sound.
pub fn situation_set_sound_filter(sound: &mut SituationSound, r#type: SituationFilterType, cutoff_hz: f32, q_factor: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundFilter(sound, r#type, cutoff_hz, q_factor) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the stereo pan for a sound [-1.0 to 1.0].
pub fn situation_set_sound_pan(sound: &mut SituationSound, pan: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundPan(sound, pan) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the pitch for a sound (resamples).
pub fn situation_set_sound_pitch(sound: &mut SituationSound, pitch: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundPitch(sound, pitch) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Apply a reverb effect to a sound.
pub fn situation_set_sound_reverb(sound: &mut SituationSound, enabled: bool, room_size: f32, damping: f32, wet_mix: f32, dry_mix: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundReverb(sound, enabled, room_size, damping, wet_mix, dry_mix) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the volume for a specific sound.
pub fn situation_set_sound_volume(sound: &mut SituationSound, volume: f32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetSoundVolume(sound, volume) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set a desired frame rate cap (0 for uncapped).
pub fn situation_set_target_fps(fps: c_int) {
    unsafe { SituationSetTargetFPS(fps) };
}

/// [Phase 2] Update sampler state.
pub fn situation_set_texture_sampler_params(texture: SituationTexture, min_filter: SituationTextureFilter, mag_filter: SituationTextureFilter, wrap_s: SituationTextureWrap, wrap_t: SituationTextureWrap) -> Result<(), SituationError> {
    let err = unsafe { SituationSetTextureSamplerParams(texture, min_filter, mag_filter, wrap_s, wrap_t) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Pins the CURRENT thread (logical CPU bitmask, bits 0..63)
pub fn situation_set_thread_affinity(core_mask: u64) -> Result<(), SituationError> {
    let err = unsafe { SituationSetThreadAffinity(core_mask) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set affinity; optional previous mask
pub fn situation_set_thread_affinity_ex(core_mask: u64, out_previous: &mut u64) -> Result<(), SituationError> {
    let err = unsafe { SituationSetThreadAffinityEx(core_mask, out_previous) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the period of an oscillator.
pub fn situation_set_timer_oscillator_period(oscillator_id: c_int, period_seconds: f64) -> Result<(), SituationError> {
    let err = unsafe { SituationSetTimerOscillatorPeriod(oscillator_id, period_seconds) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Route a procedural tone to the active graph's SFX sound source.
pub fn situation_set_tone_routing(handle: SituationToneHandle, route_to_graph: bool) -> Result<(), SituationError> {
    let err = unsafe { SituationSetToneRouting(handle, route_to_graph) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the minimum log level for output filtering.
pub fn situation_set_trace_log_level(logType: c_int) {
    unsafe { SituationSetTraceLogLevel(logType) };
}

/// Enable or disable VSync (vertical synchronization).
pub fn situation_set_vsync(enable: bool) {
    unsafe { SituationSetVSync(enable) };
}

/// Tier B storage-only attachment defaults.
pub fn situation_set_virtual_display_attachment_defaults(display_id: c_int, defaults: &mut SituationVirtualDisplayAttachmentDefaults) -> Result<(), SituationError> {
    let err = unsafe { SituationSetVirtualDisplayAttachmentDefaults(display_id, defaults) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Mark a virtual display as needing to be re-rendered.
pub fn situation_set_virtual_display_dirty(display_id: c_int, is_dirty: bool) {
    unsafe { SituationSetVirtualDisplayDirty(display_id, is_dirty) };
}

/// SOLID idle tint (normalized by compositor).
pub fn situation_set_virtual_display_fallback_color(display_id: c_int, color: ColorRGBA) {
    unsafe { SituationSetVirtualDisplayFallbackColor(display_id, color) };
}

/// SOLID, COLORBURST, or PATTERN when idle.
pub fn situation_set_virtual_display_fallback_mode(display_id: c_int, mode: SituationVDFallbackMode) {
    unsafe { SituationSetVirtualDisplayFallbackMode(display_id, mode) };
}

/// Set idle threshold for compositor fallback (Phase 2a).
pub fn situation_set_virtual_display_idle_threshold(display_id: c_int, threshold_seconds: f64) {
    unsafe { SituationSetVirtualDisplayIdleThreshold(display_id, threshold_seconds) };
}

/// Full standby tuning + PATTERN mode.
pub fn situation_set_virtual_display_pattern_config(display_id: c_int, config: &mut SitVdStandbyConfig) {
    unsafe { SituationSetVirtualDisplayPatternConfig(display_id, config) };
}

/// Toggle bitmask + PATTERN fallback (plan §3.4).
pub fn situation_set_virtual_display_pattern_layers(display_id: c_int, pattern_layers: c_int) {
    unsafe { SituationSetVirtualDisplayPatternLayers(display_id, pattern_layers) };
}

/// Set the scaling/filtering mode for a virtual display.
pub fn situation_set_virtual_display_scaling_mode(display_id: c_int, scaling_mode: SituationScalingMode) -> Result<(), SituationError> {
    let err = unsafe { SituationSetVirtualDisplayScalingMode(display_id, scaling_mode) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the window to be focused.
pub fn situation_set_window_focused() {
    unsafe { SituationSetWindowFocused() };
}

/// Set the icon for the window (single image).
pub fn situation_set_window_icon(image: SituationImage) {
    unsafe { SituationSetWindowIcon(image) };
}

/// Set the icon for the window (multiple sizes).
pub fn situation_set_window_icons(count: c_int) {
    unsafe { SituationSetWindowIcons(count) };
}

/// Set the window maximum dimensions.
pub fn situation_set_window_max_size(width: c_int, height: c_int) {
    unsafe { SituationSetWindowMaxSize(width, height) };
}

/// Set the window minimum dimensions.
pub fn situation_set_window_min_size(width: c_int, height: c_int) {
    unsafe { SituationSetWindowMinSize(width, height) };
}

/// Set the window to be fullscreen on a specific monitor.
pub fn situation_set_window_monitor(monitor_id: c_int) {
    unsafe { SituationSetWindowMonitor(monitor_id) };
}

/// Set window opacity [0.0f to 1.0f].
pub fn situation_set_window_opacity(opacity: f32) {
    unsafe { SituationSetWindowOpacity(opacity) };
}

/// Set the window position on the screen.
pub fn situation_set_window_position(x: c_int, y: c_int) {
    unsafe { SituationSetWindowPosition(x, y) };
}

/// Set the window dimensions.
pub fn situation_set_window_size(width: c_int, height: c_int) {
    unsafe { SituationSetWindowSize(width, height) };
}

/// Set window configuration state using flags (additive).
pub fn situation_set_window_state(flags: u32) {
    unsafe { SituationSetWindowState(flags) };
}

/// Set the flag profiles for when the window is focused vs. unfocused.
pub fn situation_set_window_state_profiles(active_flags: u32, inactive_flags: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationSetWindowStateProfiles(active_flags, inactive_flags) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Set the title for the window.
pub fn situation_set_window_title() {
    unsafe { SituationSetWindowTitle() };
}

/// Create connected virtual out→in pair. Returns input device_id for SituationEnableMidiControl().
pub fn situation_setup_virtual_midi_loopback(out_input_device_id: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationSetupVirtualMidiLoopback(out_input_device_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Show the mouse cursor.
pub fn situation_show_cursor() {
    unsafe { SituationShowCursor() };
}

/// Blocking UI message box; for fatal init errors.
pub fn situation_show_message_box(title: &CStr, message: &CStr) {
    unsafe { SituationShowMessageBox(title.as_ptr(), message.as_ptr()) };
}

/// [Main thread] Shut down the library and release all resources.
pub fn situation_shutdown() {
    unsafe { SituationShutdown() };
}

/// Create a new sound by copying the raw PCM data from a source.
pub fn situation_sound_copy(source: &mut SituationSound, out_destination: &mut SituationSound) -> Result<(), SituationError> {
    let err = unsafe { SituationSoundCopy(source, out_destination) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Crop a sound's PCM data in-place to a new range.
pub fn situation_sound_crop(sound: &mut SituationSound, initFrame: u64, finalFrame: u64) -> Result<(), SituationError> {
    let err = unsafe { SituationSoundCrop(sound, initFrame, finalFrame) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Export the sound's raw PCM data to a WAV file.
pub fn situation_sound_export_as_wav(sound: &mut SituationSound, fileName: &CStr) -> Result<(), SituationError> {
    let err = unsafe { SituationSoundExportAsWav(sound, fileName.as_ptr()) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Start capturing audio input with default format.
pub fn situation_start_audio_capture(callback: SituationAudioCaptureCallback, user_data: *mut c_void) -> Result<(), SituationError> {
    let err = unsafe { SituationStartAudioCapture(callback, user_data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Start capturing with explicit sample rate and channel count.
pub fn situation_start_audio_capture_ex(callback: SituationAudioCaptureCallback, user_data: *mut c_void, sample_rate: u32, channels: u32) -> Result<(), SituationError> {
    let err = unsafe { SituationStartAudioCaptureEx(callback, user_data, sample_rate, channels) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Start learning: next CC received maps to this param. Scaling: 0=linear, 1=log, 2=dB, 3=discrete. Times out after 5s.
pub fn situation_start_midi_learn(graph: *mut SituationAudioGraph, handle: SituationNodeHandle, control_index: c_int, param_name: &CStr, min_value: f32, max_value: f32, scaling: c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationStartMidiLearn(graph, handle, control_index, param_name.as_ptr(), min_value, max_value, scaling) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Stop all currently playing sounds.
pub fn situation_stop_all_loaded_sounds() -> Result<(), SituationError> {
    let err = unsafe { SituationStopAllLoadedSounds() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Stop all active tones (triggers release on each).
pub fn situation_stop_all_tones() {
    unsafe { SituationStopAllTones() };
}

/// Stop audio capture and release the input device.
pub fn situation_stop_audio_capture() {
    unsafe { SituationStopAudioCapture() };
}

/// Stop a specific sound from playing.
pub fn situation_stop_loaded_sound(sound: &mut SituationSound) -> Result<(), SituationError> {
    let err = unsafe { SituationStopLoadedSound(sound) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Gracefully stop a tone by triggering its release envelope. Invalid handles are ignored.
pub fn situation_stop_tone(handle: SituationToneHandle) {
    unsafe { SituationStopTone(handle) };
}

/// Submits a job with priority flags and optional data payload.
pub fn situation_submit_job_ex(pool: *mut SituationThreadPool, data: *mut c_void, data_size: usize, flags: SituationJobFlags) -> SituationJobId {
    unsafe { SituationSubmitJobEx(pool, data, data_size, flags) }
}

/// Submit a render list for async recording on a worker thread.
pub fn situation_submit_render_list(pool: *mut SituationThreadPool, list: SituationRenderList, user_data: *mut c_void) -> SituationJobId {
    unsafe { SituationSubmitRenderList(pool, list, user_data) }
}

/// Take a screenshot. fileName is the base name (no extension) or NULL for auto-name. Extension added from format setting.
pub fn situation_take_screenshot() -> Result<(), SituationError> {
    let err = unsafe { SituationTakeScreenshot() };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Close and destroy the virtual loopback devices.
pub fn situation_teardown_virtual_midi_loopback() {
    unsafe { SituationTeardownVirtualMidiLoopback() };
}

/// Get the period of an oscillator in seconds.
pub fn situation_timer_get_oscillator_period(oscillator_id: c_int) -> f64 {
    unsafe { SituationTimerGetOscillatorPeriod(oscillator_id) }
}

/// Get the current binary state (0 or 1) of an oscillator.
pub fn situation_timer_get_oscillator_state(oscillator_id: c_int) -> bool {
    unsafe { SituationTimerGetOscillatorState(oscillator_id) }
}

/// Get the total number of times an oscillator has triggered.
pub fn situation_timer_get_oscillator_trigger_count(oscillator_id: c_int) -> u64 {
    unsafe { SituationTimerGetOscillatorTriggerCount(oscillator_id) }
}

/// Get progress [0.0 to 1.0+] of the interval since the last successful ping.
pub fn situation_timer_get_ping_progress(oscillator_id: c_int) -> f64 {
    unsafe { SituationTimerGetPingProgress(oscillator_id) }
}

/// Get the previous frame's state of an oscillator.
pub fn situation_timer_get_previous_oscillator_state(oscillator_id: c_int) -> bool {
    unsafe { SituationTimerGetPreviousOscillatorState(oscillator_id) }
}

/// Get the total time elapsed since initialization.
pub fn situation_timer_get_time() -> f64 {
    unsafe { SituationTimerGetTime() }
}

/// Check if an oscillator's state has changed this frame.
pub fn situation_timer_has_oscillator_updated(oscillator_id: c_int) -> bool {
    unsafe { SituationTimerHasOscillatorUpdated(oscillator_id) }
}

/// Check if an oscillator's period has elapsed since the last ping.
pub fn situation_timer_ping_oscillator(oscillator_id: c_int) -> bool {
    unsafe { SituationTimerPingOscillator(oscillator_id) }
}

/// Toggle window between borderless and decorated mode.
pub fn situation_toggle_borderless_windowed() {
    unsafe { SituationToggleBorderlessWindowed() };
}

/// Toggle window between fullscreen and windowed mode.
pub fn situation_toggle_fullscreen() {
    unsafe { SituationToggleFullscreen() };
}

/// Toggle flags in the current profile and apply the result.
pub fn situation_toggle_window_state_flags(flags_to_toggle: SituationWindowStateFlags) -> Result<(), SituationError> {
    let err = unsafe { SituationToggleWindowStateFlags(flags_to_toggle) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Re-sort the graph processing order after topology changes. Call from the main thread after CreateNode/DestroyNode/CreatePatch/RemovePatch.
pub fn situation_topological_sort(graph: *mut SituationAudioGraph) -> Result<(), SituationError> {
    let err = unsafe { SituationTopologicalSort(graph) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Unload audio by handle and free resources.
pub fn situation_unload_audio(handle: SituationSoundHandle) {
    unsafe { SituationUnloadAudio(handle) };
}

/// Unload the file path list returned by SituationLoadDroppedFiles.
pub fn situation_unload_dropped_files(paths: *const *const c_char, count: c_int) {
    unsafe { SituationUnloadDroppedFiles(paths, count) };
}

/// Frees CPU font data, glyph metrics, and owned GPU atlas (not the built-in default).
pub fn situation_unload_font(font: SituationFont) {
    unsafe { SituationUnloadFont(font) };
}

/// Unload an image's pixel data from memory.
pub fn situation_unload_image(image: SituationImage) {
    unsafe { SituationUnloadImage(image) };
}

/// Frees all GPU and CPU resources associated with a loaded model.
pub fn situation_unload_model(model: &mut SituationModel) {
    unsafe { SituationUnloadModel(model) };
}

/// Unload a graphics shader pipeline and free its GPU resources.
pub fn situation_unload_shader(shader: &mut SituationShader) {
    unsafe { SituationUnloadShader(shader) };
}

/// Unload a sound and free its resources.
pub fn situation_unload_sound(sound: &mut SituationSound) {
    unsafe { SituationUnloadSound(sound) };
}

pub fn situation_update() {
    unsafe { SituationUpdate() };
}

/// Update data in a GPU buffer.
pub fn situation_update_buffer(buffer: SituationBuffer, offset: usize, size: usize, data: *mut c_void) -> Result<(), SituationError> {
    let err = unsafe { SituationUpdateBuffer(buffer, offset, size, data) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// [Main thread] Update all internal timers (frame timer, temporal system). Call after polling events.
pub fn situation_update_timers() {
    unsafe { SituationUpdateTimers() };
}

/// Returns first missing/wrong-type uniform, or SUCCESS if all resolved.
pub fn situation_validate_shader_uniforms(shader: SituationShader, table: &mut SituationUniformExpectation, table_count: c_int, error_buf: &CStr, error_buf_size: usize) -> Result<(), SituationError> {
    let err = unsafe { SituationValidateShaderUniforms(shader, table, table_count, error_buf.as_ptr(), error_buf_size) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// CC (e.g. mod wheel, expression).
pub fn situation_virtual_midi_control_change(channel: u8, controller: u8, value: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiControlChange(channel, controller, value) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Inject note-off on channel 0 (legacy wrapper).
pub fn situation_virtual_midi_note_off(note: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiNoteOff(note) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Channel-aware note-off (0-15).
pub fn situation_virtual_midi_note_off_ex(channel: u8, note: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiNoteOffEx(channel, note) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Inject note-on on channel 0 (legacy wrapper).
pub fn situation_virtual_midi_note_on(note: u8, velocity: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiNoteOn(note, velocity) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Channel-aware note-on (0-15).
pub fn situation_virtual_midi_note_on_ex(channel: u8, note: u8, velocity: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiNoteOnEx(channel, note, velocity) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Pitch bend 0..16383 (center 8192).
pub fn situation_virtual_midi_pitch_bend(channel: u8, bend: i16) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiPitchBend(channel, bend) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Program change on channel 0-15.
pub fn situation_virtual_midi_program_change(channel: u8, program: u8) -> Result<(), SituationError> {
    let err = unsafe { SituationVirtualMidiProgramChange(channel, program) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Blocks until all queued jobs are finished.
pub fn situation_wait_for_all_jobs(pool: *mut SituationThreadPool) {
    unsafe { SituationWaitForAllJobs(pool) };
}

/// Waits for a specific job to complete (O(1) check).
pub fn situation_wait_for_job(pool: *mut SituationThreadPool, job_id: SituationJobId) -> Result<(), SituationError> {
    let err = unsafe { SituationWaitForJob(pool, job_id) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Check if the application should close (e.g., user clicked X).
pub fn situation_window_should_close() -> bool {
    unsafe { SituationWindowShouldClose() }
}

/// Scale Q (chroma amplitude); preserve luma and phase.
pub fn situation_ypq_adjust_chroma(color: ColorYPQA, chroma_factor: f32) -> ColorYPQA {
    unsafe { SituationYpqAdjustChroma(color, chroma_factor) }
}

/// Scale Y (luma); preserve phase and chroma.
pub fn situation_ypq_adjust_luma(color: ColorYPQA, luma_factor: f32) -> ColorYPQA {
    unsafe { SituationYpqAdjustLuma(color, luma_factor) }
}

/// Rotate hue; P shifts by byte steps mod 256.
pub fn situation_ypq_adjust_phase(color: ColorYPQA, phase_shift: c_int) -> ColorYPQA {
    unsafe { SituationYpqAdjustPhase(color, phase_shift) }
}

pub fn situation_ypq_analyze_rgb_mapping(out: &mut SituationYpqRgbMappingStats) -> Result<(), SituationError> {
    let err = unsafe { SituationYpqAnalyzeRgbMapping(out) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Reduce chroma if linear RGB would clip.
pub fn situation_ypq_clamp_in_gamut(ypq: ColorYPQf) -> ColorYPQf {
    unsafe { SituationYpqClampInGamut(ypq) }
}

/// Weighted distance in YPQ space.
pub fn situation_ypq_distance(a: ColorYPQA, b: ColorYPQA) -> f32 {
    unsafe { SituationYpqDistance(a, b) }
}

/// Per-channel tolerance compare.
pub fn situation_ypq_equals(a: ColorYPQA, b: ColorYPQA, tolerance: u8) -> bool {
    unsafe { SituationYpqEquals(a, b, tolerance) }
}

/// Normalized chroma amplitude [0, 1].
pub fn situation_ypq_get_chroma(color: ColorYPQA) -> f32 {
    unsafe { SituationYpqGetChroma(color) }
}

/// Hue in degrees [0, 360).
pub fn situation_ypq_get_hue_degrees(color: ColorYPQA) -> f32 {
    unsafe { SituationYpqGetHueDegrees(color) }
}

/// Normalized luma [0, 1].
pub fn situation_ypq_get_luma(color: ColorYPQA) -> f32 {
    unsafe { SituationYpqGetLuma(color) }
}

/// Interpolate YPQ; phase uses shortest arc on the hue wheel.
pub fn situation_ypq_lerp(a: ColorYPQA, b: ColorYPQA, t: f32) -> ColorYPQA {
    unsafe { SituationYpqLerp(a, b, t) }
}

/// Float YPQ → 8-bit ColorYPQA.
pub fn situation_ypq_quantize(ypq: ColorYPQf) -> ColorYPQA {
    unsafe { SituationYpqQuantize(ypq) }
}

pub fn situation_ypq_slice_duplicate_count(axis: c_char, value: c_int, out_dup: &mut c_int) -> Result<(), SituationError> {
    let err = unsafe { SituationYpqSliceDuplicateCount(axis, value, out_dup) };
    if err == SituationError::SITUATION_SUCCESS {
        Ok(())
    } else {
        Err(err)
    }
}

/// Float YPQ → A2R10G10B10 packed pixel (10-bit SDR).
pub fn situation_ypq_to_rgb10_packed(ypq: ColorYPQf) -> u32 {
    unsafe { SituationYpqToRgb10Packed(ypq) }
}

/// Float YPQ → PQ-encoded A2R10G10B10 (HDR10 swapchain).
pub fn situation_ypq_to_rgb10_packed_hdr(ypq: ColorYPQf) -> u32 {
    unsafe { SituationYpqToRgb10PackedHdr(ypq) }
}

/// Float YPQ → 10-bit RGBA (linear YIQ, clamped).
pub fn situation_ypq_to_rgba10(ypq: ColorYPQf) -> ColorRGBA10 {
    unsafe { SituationYpqToRgba10(ypq) }
}

pub fn situation_begin_frame() {
    situation_poll_input_events();
    situation_update_timers();
}

pub fn situation_success(err: SituationError) -> bool {
    err == SituationError::SITUATION_SUCCESS
}