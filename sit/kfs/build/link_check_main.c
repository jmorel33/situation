/**
 * @file build/link_check_main.c
 * @brief Links against libkfs.a to verify the public API resolves (§5.7).
 */
#include "kfs/kfs.h"

int main(void) {
    GameDB* db = NULL;
    (void)db;
    return 0;
}